---
description: Bitget 코인 트랙 — bitget/ 작업 시 격리·Handoff·세션 종료 (Track B)
globs: bitget/**
alwaysApply: false
---

# Track B — Bitget 코인 (globs: bitget/**)

**이 규칙이 켜지면 KR/US `docs/work_phases/` Handoff로 구현하지 말 것.**

## SSOT

- 진행·Handoff: `bitget/docs/work_phases/`
- **병렬 창**: `16_멀티창_레인_프로토콜.md` · `lanes/ACTIVE_LANES.md` · 본문=`lanes/<LANE_ID>/`
- 구조·CAT: `bitget/docs/claude_project/`
- P0 헌법: `bitget/docs/13_institutional_grade_audit_and_roadmap.md`

## 수정 범위

- **허용**: `bitget/**` only
- **읽기 전용 import**: 루트 `practitioner_intelligence`, `reports/*`, `llm_gemini_core`, `telegram_message_queue`
- **절대 수정 금지**: 루트 `forward/`, `factory_pipelines.py`, `system_auto_pilot.py`, `performance_budget_governor.py`, `deploy/systemd/dante-*`

주식 SSOT를 bitget에 무리 복사 금지 — Adapter 패턴 (`governance/meta_consumer.py` 등).

## 세션 시작

1. `lanes/ACTIVE_LANES.md` — **내 레인 ID**
2. `lanes/<LANE_ID>/NEXT_ACTION.md`
3. `CLAUDE_TO_CURSOR.md` (= 유일 spec · 내 sub-phase)
4. OUTBOX 본문: `lanes/<LANE_ID>/CURSOR_TO_CLAUDE.md` (루트 OUTBOX는 인덱스만)

**금지**: 다른 레인 `lanes/*/CURSOR_TO_CLAUDE|09|NEXT_*` 덮어쓰기.

## 협업

- Claude Handoff 없이 구현 **금지**
- 한 세션 = sub-phase **하나** = 레인 **하나** · targeted diff only
- Claude OK 전 Done **금지**
- 동일 에러 3회 → 중단 · 로그 요약 · Claude spec 질의

## 리스크

- `ENABLE_REAL_EXECUTION`, portfolio MDD, MAX_LEVERAGE, KILL_SWITCH 변경 시 **극보수적** diff
- execution_safety 13-gate 우회 **금지**

## B-2 shadow·관측 (production 변경 금지)

- `WALK_FORWARD_PROMOTION_BLOCK_ENABLED` **임의 활성화 금지**
- production Kelly·mult 변경 금지 — shadow 격리 테스트만
- 로그·테이블 read + 보고만

## 세션 종료 (Bitget)

- [ ] `lanes/<ID>/` OUTBOX·NEXT·STEP·09
- [ ] `lanes/ACTIVE_LANES.md` 자기 행
- [ ] 루트 `NEXT_ACTION` / `09` / `CURSOR_TO_CLAUDE` **인덱스만**
- [ ] `track_b_05_진행로그.md` 해당 sub 섹션
- [ ] `track_b_00_전체현황판.md` (필요 시)
- [ ] 디렉터 3줄 (레인 ID 포함)

부팅: `17_Cursor_세션_부팅_가이드.md` §Track B · 첫 메시지에 `레인: LANE_XXX` 명시

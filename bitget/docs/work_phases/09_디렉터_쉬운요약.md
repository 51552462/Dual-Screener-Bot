# 디렉터용 쉬운 요약 (비개발자 OK)

> 갱신: 2026-08-25 · VPS 파일럿 준비 · push 대기

## 지금 한 줄
시운전(파일럿)은 **코인 서버(VPS)**에서 돌립니다. 그 전에 이 PC의 최신 코드를 서버에 올려야 합니다.

신호등: 🟢 HIST-1 코드 · 🟡 **commit/push → VPS 실행** · 🟡 R1a 관측 계속

### 당신이 할 일
1. Cursor에: `HIST-1+파일럿 스크립트 commit/push 해줘`
2. VPS에서 아래 3줄 실행
3. 나온 JSON을 이 창에 붙여넣기

```bash
cd ~/dante_bots/Dual-Screener-Bot
git pull
export BITGET_DB_STORAGE_PATH=/var/lib/quant-bitget/data
bash bitget/deploy/run_full_bt_hist_pilot.sh
```

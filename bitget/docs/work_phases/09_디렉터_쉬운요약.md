# 디렉터용 쉬운 요약 (비개발자 OK)

> **갱신**: 2026-08-20 · DNA 「왜」진단 · **Claude OK** · 서버 올리기만

---

## 지금 한 줄

> DNA 칸이 **왜**인지 말합니다. Claude OK 났으니 **서버에 올린 뒤** 텔레그램에 「재료가 아직 덜 모였어요」(🟡)가 뜨는지 한 번만 보면 됩니다. 실전·funding·MDD5%는 손대지 마세요.

신호등: 🟢 괜찮음 · 🟡 기다림 · 🔴 손볼 구멍 · ⬜ 나중(실전·funding·MDD5% 등)

### 텔레그램이 보내는 것 (매일 20:00 KST)

1. **대시보드** — 진행률 바 + 4칸 체크리스트 (쉬운 말) · DNA는 상태별 1줄
2. **숫자 메모** — OPEN/CLOSED · Cos · **DNA진단 state / n_mfe8 / action**
3. **복붙** — DNA가 `DIRECTOR_SSH_CHECK`/`REPORT_TO_CLAUDE`이거나 다른 🔴일 때

수동: `bash bitget/deploy/bitget.sh --post-deploy-obs-digest`  
끄기: `POST_DEPLOY_OBS_DIGEST_ENABLED=false`  
DNA 진단만 끄기: `POST_DEPLOY_OBS_DNA_DIAGNOSIS_ENABLED=false` (예전 RANK 문구로)

### 지금 서버 실측이면 DNA는?

「재료가 아직 덜 모였어요 – 계속 기다리면 돼요」(🟡) — 고치라는 뜻이 **아님**.

---

## I-GMM-DNA-01 — 왜 스캔은 오는데 매매(OPEN)는 0이었나

| 항목 | 내용 |
|------|------|
| **증상** | 텔레그램 신호는 하루 ~1000건 · 가상 장부 OPEN/CLOSED = 0 |
| **원인** | DNA 데이터는 **GMM 이름**으로 저장됐는데, 매매 게이트는 **다른 이름**만 읽음 → 점수 항상 0 |
| **조치** | 두 이름을 잇는 어댑터 + (배포 시) 1회 sync |
| **2026-08-17** | **배포 완료** (디렉터). 로컬 문서의 “배포 대기”는 낡은 말. |
| **지금 할 일** | 서버에서 OPEN 숫자 · `Cos_eff` · DNA 키 확인. RANK 키가 **없을 때만** sync `--force`. |

---

## 서버에 “했는지 / 안 했는지” (이번 창에서 알 수 있는 것)

| 항목 | 했는가 |
|------|--------|
| I-GMM 코드 배포 | **했다** (디렉터 확인) |
| 장부 OPEN이 생겼는지 | **아직 모름** — 서버 SQL만 앎 |
| 로그 자동 정리 (L-1) | **모름** — 설치 기록 없음 |
| DB 백업 (L-2) | **모름** — 설치 기록 없음 |
| AI 감사관 + 리포트봇 기동 | **모름** — 기동 기록 없음 |

---

## 0. 지금 내 Bitget 팩토리가 하는 일 (기준선)

가상매매(paper)만 돌아갑니다. 실제 거래소 주문은 꺼져 있습니다.

대략 흐름은 이렇습니다.

1. **스캐너**가 코인 후보를 고른다 (슈퍼노바·마스터스캐너 등)
2. 후보가 **try_add**를 통과하면 **가상 포지션 OPEN** (`bitget_forward_trades`)
3. 청산 조건이 되면 **CLOSED** · 손익·켈리·국면은 **`bitget_system_config.sqlite`** 설정값을 따른다
4. 주간 리포트·AI 감사관(23:50)·4-track 관측은 **옆에서 보고만** 하거나 로그를 남긴다

**이번에 바꾼 것** = 위 1~3의 **돈·주문·켈리 공식 자체**를 새로 짠 게 아니라, **어디서 걸러내고, 무엇을 기록하고, 설정을 누가 바꿀 수 있는지**에 조각을 몇 개 끼운 것입니다.

---

## 1. C-1 — 이상한 캔들 걸러내기 (스캐너 단계)

| 항목 | 내용 |
|------|------|
| **어디에 붙었나** | `supernova_hunter`, `master_scanner` — **매수 후보를 만들기 직전** |
| **무엇을 하나** | 최근 봉이 비정상(갭·변동폭이 ATR 대비 너무 큼)이면 **그 코인은 이번 스캔에서 후보 목록에 안 넣음** |
| **내 구조에 영향** | try_add·켈리·MDD·실주문 경로 **그대로**. 후보 리스트에 **덜 실림** |
| **안 바뀐 것** | try_add 직전 `price_sanity_gate`(CAT-N) — **별도 안전망 그대로 유지** |
| **지금 체감** | paper 돌리는 중이면 “이상 캔들 때문에 후보에서 빠진 횟수”가 ops 로그에 쌓임 |

---

## 2. C-1b — 위 걸러낸 횟수 주간 집계 (보기만)

| 항목 | 내용 |
|------|------|
| **어디에 붙었나** | **주간 배치** `weekly_evolution` (워크포워드 섀도우 다음 단계) |
| **무엇을 하나** | 지난 7일 `bad_tick_filtered` 로그를 세서 **주 1회 요약 숫자**를 ops에 남김 |
| **내 구조에 영향** | **없음** (읽기만, 설정·매매 변경 없음) |
| **왜 했나** | C-1이 실제로 얼마나 작동하는지 숫자로 보려고 (수동 확인 대신) |

---

## 3. D-1 — AI가 “설정 바꾸자”는 글을 JSON으로 저장 (아직 적용 안 함)

| 항목 | 내용 |
|------|------|
| **어디에 붙었나** | `ai_overseer` — 하루 Gemini 감사 리포트 **뒤** |
| **무엇을 하나** | AI 답변 안에 JSON 제안 블록이 있으면 검증 후 **`bitget_llm_proposals` 테이블에 저장** |
| **내 구조에 영향** | **`bitget_system_config`는 안 바뀜**. 제안만 DB에 쌓임 |
| **안 바뀐 것** | `weekly_action_plan` — 여전히 **자동으로 설정 안 바꿈** (리포트 문구만) |

---

## 4. D-1b — AI 제안이 주에 몇 번 왔는지 집계 (보기만)

| 항목 | 내용 |
|------|------|
| **어디에 붙었나** | 주간 배치 — C-1b 직후 |
| **무엇을 하나** | `bitget_llm_proposals` + 파싱 실패 횟수를 **주 1회 요약** |
| **내 구조에 영향** | **없음** (관측만) |

---

## 5. D-2 — 텔레그램으로 “승인”해야만 설정이 바뀜 (🔴 설정 변경 경로)

| 항목 | 내용 |
|------|------|
| **어디에 붙었나** | `proposal_approval_bg.py` — D-1에서 저장된 제안을 대상 |
| **무엇을 하나** | REPORT_BOT 허용 채팅에서 `/proposal_approve <번호>` → 제안에 적힌 키만 **`set_config_value`로 반영** · `/proposal_reject` → **설정 안 건드림** |
| **poll 배선 (2026-08-04)** | `ai_overseer` 루프가 **약 30초마다** REPORT_BOT `getUpdates`로 위 명령을 읽음 → 서버가 처리·답장 |
| **Claude 검증** | poll 배선 **2026-08-04 OK** — gate 로직 변경 없음 확인 |
| **켜려면** | 서버에서 **ai_overseer 프로세스**가 돌아가야 함 + `.env`에 `REPORT_BOT_TOKEN`·`REPORT_BOT_CHAT_ID` + `AI_PROPOSAL_APPROVAL_GATE_ENABLED=true` |
| **끄려면** | `AI_PROPOSAL_APPROVAL_POLL_ENABLED=false` (명령 수신만 끔) 또는 `AI_PROPOSAL_APPROVAL_GATE_ENABLED=false` (승인 자체 끔) |

---

## 6. D-3 — 주간 비용·paper/실전 차이 관측 (2026-08-04)

| 항목 | 내용 |
|------|------|
| **D-3a (켜짐)** | 주간 배치가 Gemini 호출 수(캐시 proxy)·paper 거래대금을 ops에 **숫자로만** 기록 |
| **USD 비용·수수료** | **null** — 단가·요율 SSOT가 코드에 없어서 임의 추정 안 함 |
| **D-3b (꺼짐)** | paper vs 실전 PnL 비교 **함수만** 존재, cron/주간배치 **미연결** |
| **Claude 검증** | D-3 **2026-08-04 OK** — 비용·수수료 null이 맞음(단가 없음) |
| **내 구조에 영향** | **없음** (읽기·로그만) |

---

## 7. 이번 작업들로 **전혀** 안 건드린 것 (중요)

- 실매매 `ENABLE_REAL_EXECUTION`
- A-1~A-5 포트폴리오 MDD·켈리 상한·config 거부 규칙
- B-2 deathmatch **실제 배분** (shadow만)
- C-2 funding 손익 반영 (여전히 defer)
- 스캐너 시그널 수식·청산 엔진·주식(KR/US) 코드
- MetaGovernor가 국면·켈리를 **직접** 쓰는 기존 경로

---

## 8. 지금 당신이 알아두면 되는 한 줄

> **DNA 이름 안 맞아서 가상매매가 0이었던 구멍은 고쳐 배포됨. 지금은 장부에 자리가 생기는지만 보면 됨. 실전·funding·MDD 5%는 다음이 아님.**

---

## 9. 다음 단계

| 순서 | 할 일 | 내 구조에 미치는 영향 |
|------|--------|----------------------|
| **1 (지금)** | 서버에서 OPEN / Cos / DNA 키 확인 | 없음 (읽기만) |
| 2 | (여유 되면) L-1·L-2·AI 감사관이 켜져 있는지 | 로그·백업·텔레그램 승인 |
| 3 | paper `06` 며칠~몇 주 | 수익 판단은 여전히 여기서 |

**다음 코드 Handoff 후보 1개**: 관측 숫자만 모아 주는 미니잡 (I-GMM-DNA-01b). 실전 켜기 아님.

**18개 팩토리 계층이 우리 CAT와 어떻게 맞는지** → `00_전체현황판.md` **「디렉터 18카테고리 ↔ CAT-MAP」** 절 참고.

---

## AI 복붙용 (다음이 필요할 때)

```
묶음D(D-1~D-3) 완료. 신규 D Handoff 없음.
다음: P2-5 후 D-3b 실배선 Go/No-Go (CAT-N 읽기 경로 재검토).
```

# Phase 9 · 묶음A: 포트폴리오 리스크 & 실행 안전

> **헌법**: `13_institutional_grade_audit_and_roadmap.md` P0/P1  
> **범위**: Bitget only · 관련 CAT: **CAT-F, CAT-N, CAT-K, CAT-L**  
> **Claude Knowledge**: `@CAT-F` `@CAT-N` `@CAT-MAP` `@CAT-CONSTANTS` `@CAT-SPOT-FUT`

---

## ⚠️ CAT 문서와의 관계

`bitget/docs/claude_project/` = 구조·SSOT·경계  
본 파일 = **묶음A 작업 스펙 + 로컬 구현 현황**  
**한 번에 전체 구현 금지** — A-1 ~ A-5 순서 · Handoff 1개씩

| 위험도 | CAT | 승인 |
|--------|-----|------|
| 🔴 Critical | F, N | 디렉터 + Claude spec |

---

## 듀얼 AI 협업 루프

> **SSOT**: `07_듀얼AI_협업루프.md` · sub-phase: **A-1 ~ A-5**

---

## 로컬 구현 현황 (2026-08-02)

| sub | 스펙 (audit) | 로컬 상태 | 비고 |
|-----|--------------|-----------|------|
| A-1 | Portfolio NAV MDD 3-tier (P0-2) | ✅ 1단계 Claude OK | gate 6 PORTFOLIO_MDD · 2~3단계 대기 |
| A-2 | Tail-risk fund 실연동 (P0-3) | ✅ 1단계 Claude OK | consumption path · 2~3단계 대기 |
| A-3 | MAX_LEVERAGE hard cap (P0-4) | ✅ 1단계 Claude OK | `resolve_max_leverage` · 2~3단계 대기 |
| A-4 | Gross notional cap (P1-5) | ✅ 1단계 Claude OK | gate 7 + nav_current · 2~3단계 대기 |
| A-5 | Config write bounds (P1-4) | ✅ 1단계 Claude OK | reject-not-clamp · 2~3단계 대기 |

**묶음A Done**: A-1~A-5 **3단계 전부** (`06` 통과) · **1단계 완료** · paper+병렬 트랙 진행 중

**병렬 (2026-08-02)**: A paper 관측 중 L/B/D 선행 착수 — `CURSOR_TO_CLAUDE.md` · Claude 합의 후 Handoff

---

## Phase 목표 (한 줄)

그룹 단위 방어 → **계좌 전체(NAV) 선제 서킷** + tail fund + leverage/notional SSOT.

---

## 실행 순서

```
A-1 → A-2 → A-3 → A-4 → A-5
(L-1 log rotation P0-1은 CAT-L · 병렬 가능 — 묶음A와 독립)
```

---

## A-1 · Portfolio NAV MDD Circuit Breaker

**CAT**: F, N · **audit**: P0-2  
**SSOT**: `trading/execution_safety.py`  
**로컬**: partial — complete tier actions + config keys + telegram alert

### 스펙

- NAV = `TREASURY_SPOT_USDT + TREASURY_FUTURES_USDT` (또는 live NAV SSOT 함수)
- tier (기본):
  - **−15%**: `size_mult` reduce (e.g. 0.5)
  - **−20%**: `block_new_entries` (try_add + execution_safety)
  - **−30%**: `halt` + urgent telegram
- **그룹 MDD −30%와 별개** — portfolio layer additive
- config keys: `PORTFOLIO_MDD_REDUCE_PCT`, `PORTFOLIO_MDD_BLOCK_PCT`, `PORTFOLIO_MDD_HALT_PCT`

### 완료 기준 (1단계)

- [ ] unit: mock NAV → tier transitions
- [ ] try_add respects block tier
- [ ] `05`·`00` 갱신
- [ ] Claude OK

---

## A-2 · Tail-Risk Fund Integration

**CAT**: F · **audit**: P0-3  
**SSOT**: `trading/tail_risk_gate.py`, treasury accounting

### 스펙

- tail fund balance **read** at NAV drawdown
- trigger: absorb virtual loss OR reduce min position size (design choice — Handoff에서 확정)
- **적립-only → 소비 path** 추가

### 완료 기준

- [ ] drawdown event debits tail fund (paper)
- [ ] gate blocks when tail fund exhausted (optional tier)
- [ ] tests + Claude OK

---

## A-3 · MAX_LEVERAGE Hard Cap

**CAT**: F, N · **audit**: P0-4  
**SSOT**: `trading/leverage_manager.py`, `execution_safety.resolve_max_leverage`

### 스펙

- `MAX_LEVERAGE` config — all resolve paths enforce
- default cap: CONSTANTS (e.g. 5 or 10)
- regression: existing `test_trading_phase5.py` pass

---

## A-4 · Gross Notional Exposure Cap

**CAT**: F, D · **audit**: P1-5  
**SSOT**: try_add gate (new or extend concentration_gate)

### 스펙

- sum(open notional) / treasury ≤ `MAX_GROSS_NOTIONAL_PCT` (e.g. 80%)
- SPOT/FUT **합산** portfolio view

---

## A-5 · Config Write-Time Validation

**CAT**: K · **audit**: P1-4  
**SSOT**: `infra/config_manager.py`

### 스펙

- save-time bounds for DYNAMIC_KELLY_RISK, MAX_LEVERAGE, cutoffs
- reject out-of-range with log (no silent clamp unless spec says)

---

## 선독 파일 (묶음A 공통)

- `trading/execution_safety.py`
- `forward/ledger.py` (try_add gates)
- `governance/meta_consumer.py`
- `infra/config_manager.py`
- `docs/13_institutional_grade_audit_and_roadmap.md` §3 P0

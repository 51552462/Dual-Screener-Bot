# Phase 10 · 묶음B: 진화 & Deathmatch

> **헌법**: audit P1-1, P2-1  
> **범위**: Bitget · **CAT-H, CAT-F, CAT-K**  
> **Claude Knowledge**: `@CAT-H` `@CAT-F` `@CAT-MAP`

---

## 로컬 구현 현황 (2026-08-01)

| sub | 스펙 | 로컬 | 비고 |
|-----|------|------|------|
| B-1 | market key `BG`→`SPOT`/`FUT` | ❌ | registry split |
| B-2 | `apply_deathmatch_allocation=True` | ❌ | **False** 현행 |
| B-3 | walk-forward pipeline | ❌ | engine6 14d partial only |
| B-4 | lifecycle + MAB explore | ⚠️ | 2026-07 DB fix ✅ |

---

## 실행 순서

```
B-1 → B-2 → B-3 → B-4
(B-2는 B-1 **필수** 선행)
```

---

## B-1 · Deathmatch Market Key Normalization

**CAT**: H · **audit**: P1-1 prerequisite

### 스펙

- unify `BG` / `SPOT` / `FUT` → SSOT enum
- migration: read old keys, write new (Adapter, no data loss)
- deathmatch report + meta registry **same key**

### 완료 기준

- [ ] architecture_checks or unit for key consistency
- [ ] no stock DB writes

---

## B-2 · Deathmatch Allocation Enable

**CAT**: H, F · **audit**: P1-1

### 스펙

- `apply_deathmatch_allocation=True` behind config flag
- **shadow period**: 4w log-only before Kelly impact (디렉터 Go/No-Go)
- loser strategies: Kelly mult → 0 or min floor

### 🔴 Critical

디렉터 Go/No-Go before production Kelly impact

---

## B-3 · Walk-Forward Validation Pipeline

**CAT**: H · **audit**: P2-1

### 스펙

- rolling train/validate window for deathmatch promotion
- integrate `validation/` or new `bitget/validation/walk_forward_bg.py`
- block promotion if OOS fail

---

## B-4 · Registry Lifecycle & Explore Budget

**CAT**: H, K

### 스펙

- RETIRED/COOLED counts feed MAB explore
- verify Bitget DB isolation (`ctx.bitget_db_path`)
- champion genesis hook regression test

---

## 선독

- `evolution/champion_genesis_bg.py`
- deathmatch modules / weekly_evolution pipeline
- `05_진행로그` 2026-07 lifecycle fix notes

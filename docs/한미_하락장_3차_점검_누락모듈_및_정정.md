# 한국·미국 퀀트 — 하락장 3차 점검 (누락 모듈 · 정정 · v3 권고)

> **범위:** KR/US 주식 (`bitget/` 제외)  
> **기준 문서:** [`한미_하락장_판별_방어_진화로직_설계.md`](./한미_하락장_판별_방어_진화로직_설계.md) v2 (2026-07-09)  
> **목적:** v2 이후 **3차 코드 전수 재조사** — 빠진 모듈, 배선 오해, stale 정정, 테스트 공백  
> **작성:** 2026-07-09 v3-audit

---

## 0. TL;DR — v2 대비 핵심 발견 7건

| # | 발견 | v2 상태 | 실제 코드 |
|---|------|---------|-----------|
| 1 | **KR breadth** | G1 "부재", P2-1 신규 | `_compute_kr_breadth()` **이미 live** — MA20 상회비율 proxy, coverage 제한적 |
| 2 | **Toxic→Fade** | §C3 "권고/개념" | **`forward/shared.py` live** — toxic match → `fade_long_to_inverse` |
| 3 | **주말 진화 스케줄** | §7.2 단일 사이클 | **`factory_pipelines weekly_master` ≠ `run_autonomous_analysis`** — 2트랙 분리 |
| 4 | **공격 부스터** | v2 미언급 | **Ace/Genesis/template_bandit** — BEAR에서도 Kelly **상향** 가능 (역시너지) |
| 5 | **복구 grace** | v2 미언급 | **`factory_recovery_grace`** — PIL Kelly=0·RETIRED **일시 해제** |
| 6 | **SIDEWAYS vs CHOP** | v2 일부 언급 | ensemble=`SIDEWAYS`, auto_pilot tuning=`CHOP` — **entry_regime 분석 오염** |
| 7 | **elastic scout hole** | G12, P1-5 | **여전히 open** — `evaluate_scout_candidate`에 regime guard 없음 |

**종합:** v2는 "방어 골격"은 정확했으나, **(a) 이미 구현된 것을 미구현으로 적은 항목**, **(b) BEAR 방어를 깨는 공격 부스터**, **(c) 스케줄러 2트랙**, **(d) 리포팅/감사 레이어**가 부족했다.

---

## 1. v2 대비 코드 정정 (Stale Correction)

### 1.1 KR breadth — "부재" → "proxy live, coverage-limited"

**파일:** `predictive_regime_ensemble.py` · `_compute_kr_breadth()` L883~

```python
# MA20 상회 비율 → breadth_ratio (50% 상회 = 1.0 중립)
# market_data.sqlite KR_<code> 표본 max 600종
# 데이터 부족 → None → breadth 팩터 미투표
```

| 항목 | v2 | 정정 |
|------|-----|------|
| G1 심각도 | 🔴 "부재" | 🟠 **coverage-limited proxy** |
| P2-1 | 6번째 팩터 신규 | **품질 KPI** (표본≥N, VKOSPI/KOSPI200 보강) |
| §3.5 KR breadth | ❌ 없음 | ⚠️ **있으나 불안정** |

**권고 KPI:** `kr_breadth` non-null rate ≥ 80% (거래일), null 시 PRI+vix 가중 재정규화 로그.

### 1.2 Toxic→Fade — 이미 live

**배선:** `forward/shared.py` L1658~ → `inverse_etf_sniper.fade_long_to_inverse` + `FADE_SIG_MARKER`

| v2 | 실제 |
|----|------|
| §C3 "확장 권고" | **운영 중** — toxic bbox match → 롱 거부 → 섹터 inverse |
| G3 숏 겹치 | fade는 inverse 슬리브 경로, blackhole과 **별도** |

### 1.3 Circuit Breaker — v2 §5.5 정정 유지

`forward/ledger.py`: −5% trip, −2% 회복 또는 3거래일 해제. **sticky ON 아님.**

### 1.4 Tail ×30 → NAV — v2 G2 유지

`system_auto_pilot` M4 주석: `CENTRAL_TREASURY` direct mutation deprecated. ×30은 config 회계, NAV 미반영 **여전히 사실**.

---

## 2. 3차에서 새로 발견한 모듈 (v2 미기재 40+)

### 2.1 공격 부스터 — BEAR 방어 **역시너지** (최우선 문서화)

| 모듈 | BEAR 동작 | 위험 |
|------|-----------|------|
| `evolution/champion_genesis.py` | `genesis_kelly_boost` → try_add Kelly 가산 | toxic precursor match 시 **BEAR에도 boost** |
| `evolution/ace_entry_bridge.py` | playbook match → cutoff 완화, Kelly×1.35 | BEAR playbook이면 **공격 throttle 우회** |
| `evolution/ace_exit_bridge.py` | time_stop/MFE relax, hold extension | fluid scale-out 78%와 **청산 정책 충돌** |
| `template_bandit.py` | Thompson mult 0.1~2.0 per template | hot mutant BEAR에서 Kelly **2×** 가능 |
| `system_auto_pilot` | `ROTATION_ADVANTAGE_ACTIVE` / `SPILLOVER_ADVANTAGE_ACTIVE` | 주말 PF 통과 시 prebuy Kelly **2×/1.5×** — analog gate만 1차 방어 |

**v3 역시너지 추가 (§12.4 보완):**

- BEAR + genesis_kelly_boost + S1 arm → **hard skip or cap 1.0**
- BEAR + ace_entry_bridge mult >1.0 → **clamp to ≤1.0 unless V_RECOVERY analog**
- BEAR + template_bandit mult >1.2 → **regime ceiling**
- BEAR + ROTATION/SPILLOVER advantage → **require META_REGIME_KEY ∉ {BEAR,HIGH_VOL}**

### 2.2 진입 미세 팩터 (KR bear-trap vs bounce)

| 모듈 | 역할 | BEAR 리스크 |
|------|------|-------------|
| `kr_flow_factor.py` | 외국인/기관 flow → try_add bonus | dead-cat rally에 bonus |
| `short_interest_fetcher.py` | 공매도 squeeze / crowded-short penalty | short-cover rally trap |
| `dart_fetcher.py` | OpenDART 이벤트 score | 공시 shock in drawdown |
| `fundamentals_fetcher.py` | 펀더멘털 quality bonus/penalty | value trap |
| `proprietary_alpha_consumer.py` | spillover theme premium; **BEAR cap 1.02** | cap 있음 (tested) — v2 body 미언급 |

### 2.3 Toxic / 면역 스택 (전체 체인)

```
toxic_antipattern_core (bbox SSOT)
    → try_add gate (toxic block)
    → fade_long_to_inverse (live)
    → toxic_decay_bandit (US rule half-life + forgiveness_scout)
    → clustered_immune_vaccine (cluster bbox)
    → immune_evolution (weekend failed template cull)
    → template cull → ANTI_PATTERNS (auto_pilot Saturday)
    → practitioner_penalty_bridge (ZOMBIE Kelly=0)
```

| v2 | 3차 |
|----|-----|
| clustered_immune, toxic graveyard | ✅ |
| toxic_antipattern_core, toxic_decay_bandit, immune_evolution | ❌ **누락** |
| fade live | ❌ "권고"로 오기 |

### 2.4 Ace Evolution 스위트 (8모듈)

| 파일 | 역할 |
|------|------|
| `evolution/ace_entry_bridge.py` | 진입 Kelly/cutoff/SL ATR |
| `evolution/ace_exit_bridge.py` | 청산 time_stop/MFE |
| `evolution/ace_evolution_clamp.py` | confidence/p-value bounds |
| `evolution/ace_deathmatch_bridge.py` | ace ↔ deathmatch |
| `evolution/ace_playbook_validator.py` | OOS gate |
| `evolution/ace_fact_pack.py` | fact pack |
| `evolution/ace_evolution_ttl.py` | TTL |
| `evolution/ace_evolution_telegram.py` | 알림 |

→ v2 "진화"는 deathmatch/incubator 중심; **Ace는 live entry/exit에 직접 개입**.

### 2.5 Spillover / Rotation 방어

| 모듈 | BEAR 관련 |
|------|-----------|
| `zero_sample_spillover.py` | US forward book empty → dark-horse sector publish |
| `sector_rotation_store.py` | Markov sector prediction → rotation prebuy |
| `sector_rotation_smoothing.py` | miss-streak → disable ROTATION_ADVANTAGE |
| `rotation_sector_filter.py` | junk sector hard block |
| `spillover_calendar.py` | KR/US spillover timeline SSOT |
| `evolution/us_fluid_upstream_bridge.py` | US halt/toxic decay/zero-sample prelude |

**참고 문서 (v2 미링크):** `docs/FLUID_US_UPSTREAM_ARCHITECTURE.md`, `docs/FLUID_EVOLUTION_ARCHITECTURE.md`

### 2.6 Regime / 매크로 확장

| 모듈 | 역할 |
|------|------|
| `regime_self_heal.py` | meta↔config regime mismatch → rebuild |
| `meta_state_store.py` / `meta_state_market_db.py` | triple-store, per-market |
| `macro_matrix_incremental.py` | regime×arm incremental stats → `MACRO_EVOLUTION_MATRIX.json` |
| `proprietary_friction_store.py` | regime transition friction telemetry |
| `market_data_fetcher.py` | `MARKET_DATA_UPSTREAM_BREAKER` — 데이터 fetch CB |
| `predictive_regime_ensemble` | `CRYPTO_CANARY_*` — crypto stress → vix penalty (코인 제외 doc이지만 KR ensemble 입력) |

### 2.7 Fluid / Session / Recovery

| 모듈 | BEAR 관련 |
|------|-----------|
| `fluid_time_anchor.py` | live/carry_over/halt — bars_held, bear holiday gap |
| `market_session_gate.py` | regular session gate (test: defense_lockdown) |
| **`factory_recovery_grace.py`** | **`FACTORY_RECOVERY_GRACE=1` → PIL Kelly=0·RETIRED 미적용** |
| `factory_us_health.py` | US pipeline stale → KR spillover blind |

### 2.8 Practitioner / PIL

| 모듈 | BEAR |
|------|------|
| `practitioner_penalty_bridge.py` | ZOMBIE → Kelly=0, RETIRED, DM overlay |
| `practitioner_zombie_streak.py` | N-day streak |
| `practitioner_market_profiles.py` | KR 14d / US 45~60d post-mortem window |

### 2.9 Shadow / Audit / Reporting (BEAR KPI — v2 거의 미기재)

| 모듈 | BEAR KPI 역할 |
|------|---------------|
| **`forward/deep_dive.py`** | **entry_regime별 PnL attribution, ensemble rebuild, inverse cycle** |
| `forward/deathmatch_report_section.py` | regime crossmatrix daily section |
| `forward/rotation_report_section.py` | rotation/spillover whipsaw |
| `forward/dna_autopsy.py` | closed-trade DNA autopsy |
| `forward_flow_tag_deep_dive.py` | flow-tag Kelly mult + Bayesian penalty |
| `reports/report_state_binder.py` | NAV/HWM/MDD/regime/Kelly effective SSOT |
| `reports/report_collectors.py` | inverse mode, DEFCON, hedge 5d return |
| `reports/forward_report_scalar.py` | MDD/Sharpe/Calmar |
| `reports/report_staleness_gate.py` | GREEN/YELLOW/RED freshness |
| `shadow_performance_tracker.py` | blocked counterfactual → SHADOW_PERFORMANCE |
| `overseer_audit_binder.py` | DEFENSIVE_REGIME_KEYS, Kelly anomaly |
| `ai_overseer.py` | LLM audit narrative |
| `satellite_intel_brief.py` | GLOBAL/KR/US intel blocks |
| `factory_meta_alerts.py` | MetaGovernor CRITICAL Telegram |
| `capital_deathmatch.py` | Fixed vs Kelly MDD + micro-defense |
| `weekend_grand_report.py` | monthly NAV/MDD narrative |
| `weekly_flow_report.py` / `weekly_flow_rollup.py` / `weekly_flow_pnl.py` | weekly regime PnL |
| `spillover_v28_report.py` | spillover timeline audit |
| `evolution_digest.py` | weekend evolution digest |

**PM 관점:** BEAR 방어 검증의 **1차 관측창**은 `forward/deep_dive` + `report_state_binder` + `capital_deathmatch` — v2 KPI(§10)와 연결 필요.

### 2.10 Legacy scanner arms (KR vs US — v2 얕음)

| Scanner | BEAR arm | KR/US |
|---------|----------|-------|
| `nulrim.py` S6 | 바닥턴 단기 정배열 | KR |
| `dante_krx_reverse_breakout_screener.py` | inverse breakout | KR |
| `nasdaq_dante_reverse_breakout_screener.py` | reverse breakout | US |
| `master.py` | candle-bottom + trap warning | **KR only** |
| `us_master.py` | US master variant | US |
| `ema5.py` / `us_5ema.py` | 5EMA pullback (S1 trap) | KR/US |
| `kr_bowl_forward_bridge.py` | bowl observe-only | KR shadow |
| `nulusa.py` | US nulrim S1/S2/S4 | US |

### 2.11 Validation / Sandbox

| 모듈 | BEAR |
|------|------|
| `validation/walk_forward.py` | purged K-fold OOS (offline) |
| `offline_rnd_sandbox.py` | scan-abort → background R&D |
| `time_machine_backtester.py` | historical replay |
| `mutant_oos_validator.py` | synthetic OOS — **BEAR-only gate 없음** |
| `alpha_mining_orchestrator.py` | alpha mining (root, v2 미언급) |

---

## 3. 배선(Scheduler) 진실표 — v2 오해 방지

### 3.1 두 개의 "주말 두뇌" — 반드시 분리 이해

| 트랙 | 트리거 | 주요 작업 | BEAR 관련 |
|------|--------|-----------|-----------|
| **`factory_pipelines` → `weekly_master`** | cron `factory.sh --weekly` | `macro_matrix_incremental`, `weekly_proprietary_regime`, `regime_deep_archive`, `weekly_flow_master` | PRI, macro matrix, deep archive |
| **`system_auto_pilot.run_autonomous_analysis`** | 토 10:00 daemon/cron | template cull, **immune_evolution**, **deathmatch hunt**, **champion_genesis backfill**, rotation/spillover PF gates, incubator, elastic tuning, **ROTATION/SPILLOVER advantage flags** | **핵심 BEAR evolution + 공격 unlock** |

**v2 §7.2 오류:** "주간 사이클"을 하나로 묶었으나, **deathmatch hunt / immune / genesis / advantage flags는 auto_pilot 쪽**.

### 3.2 일간 파이프라인 (factory_pipelines scan/audit)

```
scan prelude:
  meta_governor_sync → fluid_evolution_bridge.post_meta_governor_fluid_sync
  doomsday_bridge_sync
  [US] factory_us_health, us_fluid_upstream_bridge, toxic_decay_bandit
  [KR] cross_market hydrate, kr_flow prelude (via smart_money_refresh pipeline)

try_add (supernova/legacy):
  scanner_regime_ssot (inside supernova_hunter)
  10 gates + frontrun_gate + corr overlay + genesis + ace + kelly merge

daily_audit:
  track_daily_positions → deep_dive → deathmatch_report_section (crossmatrix)
  → doomsday_bridge → PIL → ai_overseer
  shadow_macro_validator (deep_dive append, not Saturday list)
```

### 3.3 `factory_scan_schedule.py` — 장중 슬롯

| 항목 | v2 | 3차 |
|------|-----|-----|
| `tail_doomsday=True` | 미언급 | **cycle-1 bowl slot only** |
| KR `master` scanner | 미언급 | US 없음 — KR bear bottom-turn |
| cycle-2 r2 | 미언급 | 2nd pass, light prelude, no doomsday tail |
| prelude full/light | 미언급 | 첫 supernova=full, r2=light — **intraday regime refresh 비대칭** |

### 3.4 v2 배선 누락 목록

| 모듈 | factory step? | 실제 호출 |
|------|---------------|-----------|
| `regime_self_heal` | ❌ | report/overseer path |
| `shadow_performance_tracker` | ❌ | auto_pilot Saturday subprocess |
| `satellite_intel_brief` | ❌ | standalone |
| `champion_genesis` / ace bridges | ❌ | try_add / exit hooks |
| `proprietary_alpha_consumer` | ❌ | supernova batch |
| `limit_up_forensics` | 별도 mode | KR bounce-trap feed |

---

## 4. Config / Env 레지스트리 (BEAR 방어 — v2 미집계)

| Key / Env | 모듈 | BEAR 용도 |
|-----------|------|-----------|
| `REGIME_TRANSITION_CLUTCH` | ensemble | Kelly ×0.1~0.3 |
| `KR_REGIME_KEY`, `US_REGIME_KEY`, `REGIME_ENSEMBLE` | ensemble persist | per-market |
| `REGIME_HEAL_MISMATCH_THRESHOLD` | regime_self_heal | SSOT heal |
| `META_KELLY_LEARN_ENABLED`, `META_KELLY_*` | regime_kelly_learner | cap learn (OFF) |
| `META_GOVERNOR_SKIP_VIX` | meta_governor | VIX p90 skip |
| `CRYPTO_CANARY_PENALTY_ENABLED` | ensemble | crypto stress penalty |
| `KR_CORR_*`, `PORTFOLIO_TARGET_VOL`, `VOL_TARGET_*` | portfolio_risk_overlay | corr + vol target |
| `KR_FLOW_*`, `KR_SHORT_*`, `KR_DART_*`, `KR_FUND_*` | KR micro-factors | entry adj |
| `FACTORY_RECOVERY_GRACE` | factory_recovery_grace | **PIL bear penalty bypass** |
| `FACTORY_ALLOW_SESSION_RESCAN` | session_deduplication | regime-change rescan |
| `GENESIS_PRECURSOR_*`, `GENESIS_PRECURSOR_ADVANTAGE_{KR\|US}` | champion_genesis | Kelly boost |
| `ENABLE_ACE_EVOLUTION_WEIGHTING`, `ACE_EVOLUTION_MULT_*` | ace bridges | entry/exit mult |
| `ROTATION_ADVANTAGE_ACTIVE`, `SPILLOVER_ADVANTAGE_ACTIVE` | auto_pilot → shared | prebuy Kelly unlock |
| `TOXIC_FADE_TARGETS` | inverse_etf_sniper | fade targets |
| `FLUID_KR_*`, `FLUID_US_*` | fluid_time_anchor | session carry |
| `PRACTITIONER_APPLY_PENALTIES` | practitioner_penalty_bridge | zombie demotion |
| `MARKET_DATA_UPSTREAM_BREAKER_*` | market_data_fetcher | data CB |
| `DEATHMATCH_APPLY_ALLOCATION` | deathmatch_allocation | group Kelly overlay |
| `OVERSEER_KELLY_LOW_THRESHOLD` | overseer_audit_binder | Kelly anomaly |
| `ELASTIC_SCOUT_MIN_STARVATION` | elastic_threshold | scout gate |

---

## 5. 테스트 커버리지 매트릭스

### 5.1 Covered (BEAR 관련)

| Test | Validates |
|------|-----------|
| `test_transcendent_exit.py` | BEAR scale-out ≥70%, ratchet RL |
| `test_doomsday_bridge.py` | DEFCON sync |
| `test_regime_analog_engine.py` | EXTREME_CRASH frontrun block |
| `test_proprietary_alpha_consumer.py` | BEAR theme cap 1.02 |
| `test_ultimate_synergy.py` | asymmetric treasury BEAR 18d |
| `test_regime_sync_heal.py` | regime alignment |
| `test_deep_evolution_immune.py` | immune_evolution |
| `test_macro_shadow_institutional.py` | shadow_macro_validator |
| `test_fluid_evolution.py`, `test_us_fluid_upstream.py` | fluid upstream |
| `test_deathmatch_*.py` | deathmatch scoring |

### 5.2 Not covered (high priority)

- `inverse_etf_sniper` kill switch / fade / dynamic cap
- `forward/ledger` CB auto-release + breadth collapse E2E
- `portfolio_risk_overlay` corr reject in BEAR book
- `elastic_threshold` scout **without** regime guard
- `champion_genesis` / `ace_entry_bridge` in BEAR regime
- **`factory_recovery_grace`** disabling PIL during crash
- `ROTATION/SPILLOVER advantage` without bear guard
- `_compute_kr_breadth` coverage / null rate
- KR vs US `WEIGHT_KR_NULRIM_S6` integration

---

## 6. v2 문서 정정 체크리스트 (merge to v3)

- [ ] G1: "KR breadth 부재" → "proxy live, coverage KPI"
- [ ] §C3 Toxic→Fade: "권고" → **live shipped**
- [ ] §7.2: scheduler **2-track** diagram
- [ ] §12.4: ace/genesis/template_bandit/recovery_grace **역시너지** 추가
- [ ] §10: reporting layer (deep_dive, report_state_binder, capital_deathmatch)
- [ ] Appendix: config registry (§4 above)
- [ ] Appendix: FLUID_US_UPSTREAM cross-ref
- [ ] Appendix: CRYPTO_CANARY (ensemble input, not trading)
- [ ] SIDEWAYS/CHOP unify recommendation

---

## 7. v3 권고 로드맵 (3차 신규)

| ID | 작업 | 근거 |
|----|------|------|
| P0-1 | BEAR regime gate on genesis/ace/template_bandit | §2.1 역시너지 |
| P0-2 | ROTATION/SPILLOVER advantage × BEAR block | §2.1, auto_pilot L1598~ |
| P0-3 | elastic scout BEAR guard (P1-5 유지) | G12 |
| P1-7 | `kr_breadth` coverage KPI + VKOSPI fallback | §1.1 |
| P1-8 | SIDEWAYS/CHOP → single `entry_regime` normalize | §0 #6 |
| P1-9 | `factory_recovery_grace` × BEAR explicit policy | §2.7 |
| P2-7 | reporting KPI dashboard wire (deep_dive → §10) | §2.9 |
| P2-8 | BEAR-only mutant OOS gate | mutant_oos_validator |
| P3-5 | ace_exit × BEAR scale-out priority rule | ace vs exit_dynamics |

---

## 8. SIDEWAYS vs CHOP — 분석 오염 상세

| 계층 | 라벨 | 소비처 |
|------|------|--------|
| ensemble / meta_governor | `SIDEWAYS` | META_REGIME_KEY, ACTION_BY_REGIME |
| system_auto_pilot tuning | `CHOP` | `{CHOP}_CHAMPION_PARAMS`, WEIGHT tuning |
| meta_state_store | CHOP→SIDEWAYS normalize | config align |
| forward_trades.entry_regime | META_REGIME_KEY 우선 | regime crossmatrix |

**리스크:** auto_pilot이 CHOP champion을 로드하지만 entry_regime은 SIDEWAYS → **BEAR bucket 통계 과소/과대**.

**권고:** `normalize_regime_key()`를 crossmatrix + deathmatch + champion load **전 경로** SSOT.

---

## 9. BEAR 방어 성숙도 — v3 재평가

| 영역 | v2 | v3 (3차) | 이유 |
|------|-----|----------|------|
| 판별 | ★★★★☆ | ★★★★☆ | KR breadth partial; CHOP drift |
| 사이징 | ★★★★☆ | ★★★☆☆ | genesis/ace/bandit **역시너지** 발견 |
| 청산 | ★★★★☆ | ★★★☆☆ | ace_exit vs scale-out conflict |
| 헤지 | ★★☆☆☆ | ★★★☆☆ | toxic fade live (+0.5) |
| 진화 | ★★★★☆ | ★★★★☆ | scheduler split clarified |
| 선취매 throttle | ★★★☆☆ | ★★☆☆☆ | rotation advantage BEAR hole |
| **리포팅/감사** | (미평가) | ★★★★☆ | deep_dive + overseer rich |
| **통합** | ★★★☆☆ | ★★★☆☆ | 공격 부스터가 방어 상쇄 |

---

## 10. 관련 문서 링크

| 문서 | 관련 |
|------|------|
| [한미_하락장_판별_방어_진화로직_설계.md](./한미_하락장_판별_방어_진화로직_설계.md) | v2 본문 |
| [장세_상승횡보하락_판별_정밀검토.md](./장세_상승횡보하락_판별_정밀검토.md) | 판별기 역사 |
| [꼬리위험_폭락방어_아키텍처_검토.md](./꼬리위험_폭락방어_아키텍처_검토.md) | tail/CB |
| [현재장세_과거장세_유사도_선취매_정확도_검토.md](./현재장세_과거장세_유사도_선취매_정확도_검토.md) | analog (6/28 — 이후 live) |
| [FLUID_US_UPSTREAM_ARCHITECTURE.md](./FLUID_US_UPSTREAM_ARCHITECTURE.md) | US upstream bear |
| [FLUID_EVOLUTION_ARCHITECTURE.md](./FLUID_EVOLUTION_ARCHITECTURE.md) | fluid evolution |
| [장세별_로직_수익방어_순위_검토.md](./장세별_로직_수익방어_순위_검토.md) | regime×logic rank |

---

## 부록 A. 3차 신규 모듈 전체 인덱스 (v2 대비 +)

```
regime_self_heal.py          meta_state_market_db.py       macro_matrix_incremental.py
proprietary_friction_store.py market_data_fetcher.py        kr_flow_factor.py
short_interest_fetcher.py    dart_fetcher.py               fundamentals_fetcher.py
proprietary_alpha_consumer.py toxic_antipattern_core.py    toxic_decay_bandit.py
immune_evolution.py          template_bandit.py           deep_evolution_deploy.py
dna_mutator.py               evolution/champion_genesis.py  evolution/ace_*.py (8 files)
evolution/deathmatch_allocation.py  evolution/us_fluid_upstream_bridge.py
zero_sample_spillover.py     sector_rotation_store.py     sector_rotation_smoothing.py
rotation_sector_filter.py    spillover_calendar.py        fluid_time_anchor.py
market_session_gate.py       factory_recovery_grace.py    factory_us_health.py
practitioner_penalty_bridge.py practitioner_zombie_streak.py
shadow_performance_tracker.py overseer_audit_binder.py     ai_overseer.py
satellite_intel_brief.py     factory_meta_alerts.py       forward/deep_dive.py
forward/deathmatch_report_section.py forward/rotation_report_section.py
forward/dna_autopsy.py       forward_flow_tag_deep_dive.py
reports/report_state_binder.py reports/report_collectors.py reports/report_staleness_gate.py
weekend_grand_report.py      weekly_flow_*.py             spillover_v28_report.py
evolution_digest.py          mutant_oos_validator.py      offline_rnd_sandbox.py
time_machine_backtester.py   alpha_mining_orchestrator.py legacy_archive/scanners/master.py
kr_bowl_forward_bridge.py    limit_up_forensics (pipeline) smart_money_refresh (pipeline)
```

---

## 부록 B. Toxic→Fade Live 시퀀스

```
try_add(code, sig_type, ...)
  → toxic bbox match (toxic_antipattern_core)
  → match_toxic_fade_target(cfg, sector)
  → fade_long_to_inverse(market, sector, tail_amount)
       → inverse_etf_sniper reserve_tail
       → forward_trades [INVERSE_ETF][TOXIC_FADE]
  → original long: return False, "TOXIC_FADE_TARGET"
  → shadow_tracking.record_blocked_trade(...)
```

---

*문서: 2026-07-09 · 3차 전수 점검 · KR/US only · v2 보완용*

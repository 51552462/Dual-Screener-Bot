# 디렉터용 쉬운 요약 — KR/US 주식 (비개발자 OK)

> **갱신**: 2026-08-03 · **factory 복구** · M-R0 구현 · Claude OK 대기

---

## 0. 지금 내 KR/US 팩토리가 하는 일 (기준선)

가상매매(paper)만 돌아갑니다. 한국·미국 주식 각각 별도 NAV·국면·포지션 쿼터를 씁니다.

대략 흐름:

1. **스크리너·시그널**이 KR/US 후보를 고른다
2. 후보가 **try_add** 6게이트를 통과하면 **가상 포지션 OPEN** (`forward_trades`)
3. 청산·켈리·국면은 **`system_config.sqlite`** + **성과예산 거버너(A-1~A-3)** 설정을 따른다
4. 일일 AI 감사관·주간 리포트는 **옆에서 보고·설명만** — 거래 공식 자체를 바꾸지 않음

**이번에 바꾼 것** = 매매 공식 새로 짠 게 아니라, **감독관 품질·LLM 라우팅·factory 크래시 복구** + **문서 작업 환경 bitget 대칭 이식**.

---

## 1. 긴급 복구 — factory가 안 떠 있던 이유

| 항목 | 내용 |
|------|------|
| **증상** | watchdog 텔레그램 알림 · `dante-factory` 재시작 반복 |
| **원인** | `inverse_etf_sniper.py` **문법 오류** (들여쓰기·`ttry:` 오타) — import 시 크래시 |
| **조치** | 741·797행 수정 · 커밋·푸시 후 서비스 정상 |
| **내 구조에 영향** | 매매 로직 변경 없음 — **실행만** 복구 |

---

## 2. M-R0 — 상시 감독관 품질 (CAT-M, 비침투)

| 항목 | 내용 |
|------|------|
| **M-R0-1** | `llm_provider_core` — task별 Gemini/Anthropic 선택 (기본 전부 Gemini, Anthropic은 킬스위치 OFF) |
| **M-R0-2** | 텔레그램 푸터 — LLM vs 규칙 기반 비율·anomaly 건수 (7일 창) |
| **M-R0-3** | meta degraded·pipeline critical 실패 시 **degraded rules-only** — LLM 호출 안 함, 규칙 감사만 발송 |
| **비침투** | 리포트 HTML·narrative·푸터만 — kelly·try_add·regime·config **쓰기 금지** |
| **D-2 분리** | `AI_CIO_AUDIT_LOG`와 섞지 않음 · `OVERSEER_QUALITY_LOG` 별도 |

---

## 3. Phase A 진행 (묶음A — 자본·리스크·국면)

| sub-phase | 상태 | 한 줄 |
|-----------|------|-------|
| A-1 성과예산 거버너 | 구현 ✅ · Claude OK ❌ | MDD 10% 소진율 tier → Kelly·쿼터 배율 |
| A-2 Kelly throttle | 구현 ✅ | merge chain 끝에 throttle 곱 |
| A-3 국면 쿼터 | 구현 ✅ · Claude OK ❌ | regime base × POSITION_QUOTA_MULT |
| A-1-R1 falsy 0.0 | 구현 ✅ · Claude OK ❌ | `0.0`을 default로 오독하던 버그 수정 |
| A-4~A-5 | 미착수 | 히스테리시스·S5 arm |

---

## 4. 이번 작업으로 **전혀** 안 건드린 것

- 실매매 `ENABLE_REAL_EXECUTION`
- B~D 묶음 (deathmatch live·AI CIO 승인·DSR)
- **bitget/** 코인 코드·config (격리 헌법)
- MetaGovernor가 국면·켈리를 쓰는 **기존 live 경로**

---

## 5. 지금 당신이 알아두면 되는 한 줄

> **factory는 문법 수정으로 다시 떴고, 감독관은 품질 KPI·degraded 모드까지 붙었지만 Claude OK 전 — 거래·켈리·MDD 공식은 A-1~A-3 코드만 있고 효과 검증 전.**

---

## 6. 다음 단계 (체크리스트)

| 순서 | 할 일 | 영향 |
|------|--------|------|
| 1 | Claude에 `CURSOR_TO_CLAUDE.md` + `05` M-R0·A-3·A-1-R1 **검증** | OK 후 Done 처리 |
| 2 | 서버 `.env` — `DB_STORAGE_PATH` **한 줄만** 확인 | watchdog·ops DB 경로 |
| 3 | paper 가상매매 관측 (MDD·tier) | `06` 3단계 효과 검증 준비 |
| 4 | A-4 Handoff — RL hysteresis **Adapter** 논의 | Claude Handoff 선행 |

**18개 팩토리 계층 ↔ CAT** → `00_전체현황판.md` · **코인 대칭 문서** → `bitget/docs/work_phases/` (내용 분리)

---

## 7. 주식 vs 코인 (Track A / B)

| | 주식 (본 레포 루트) | 코인 (`bitget/`) |
|---|---------------------|------------------|
| MDD 하드캡 | **10%** | 5% |
| 문서 SSOT | `docs/work_phases/` | `bitget/docs/work_phases/` |
| Mirror 로그 | `ARCHITECT_MIRROR.md` | `bitget/docs/work_phases/ARCHITECT_MIRROR.md` |
| 코드 공유 | **없음** (계약·패턴만 대칭) | 동일 |

---

## AI 복붙용

```
KR/US: factory 복구 + M-R0 구현 완료. CURSOR_TO_CLAUDE + 05 검증 요청.
병렬: A-3·A-1-R1 OK/수정 spec. OK 후 A-4 Handoff(히스테리시스 Adapter).
```

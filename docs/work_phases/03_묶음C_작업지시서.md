# Phase 3 — 묶음C: 데이터/스캐너/섹터/오케스트레이션

> **선행**: Phase 1·2 완료 + `05_진행로그.md`  
> **범위**: KR/US · 관련 파일 약 **100개**  
> **CAT 매핑**: CAT-C(주), CAT-B, CAT-G(C-3만)  
> **Claude Knowledge**: `@CAT-C` `@CAT-B` `@CAT-MAP` `@CAT-KR-US`  
> **통합본**: Claude Pro `03_기존묶음C` + Cursor sub-phase + 로컬 대조 (2026-08-01)

---

## ⚠️ CAT 문서와의 관계

`docs/claude_project` — **bitget/ 제외** 원칙.  
**C-3(bitget)** — Go/No-Go **디렉터 필수**. KR/US CAT-G 충돌 가능.

**실행 순서**: Claude Pro 원본은 ①bitget → ②섹터 → ③데이터 순.  
확장본: `C-1 → C-2 → [C-3?]` (bitget은 **선택·후행** — 자본 격리 검토 후)

| 위험도 | CAT | 승인 |
|--------|-----|------|
| 🟡 High | C, B | Claude spec |
| 🔴 Critical (C-3 Go 시) | G + bitget adapter | 디렉터 Go + shadow 4주 |

---

## 듀얼 AI 협업 루프 (필수 · 본 Phase)

> **전문 SSOT**: `07_듀얼AI_협업루프.md` · **본 Phase sub-phase**: `C-1` ~ `C-3` (C-3 선택)

| 단계 | 담당 | 행동 |
|------|------|------|
| 1 | **Claude Pro** | `C-x` Handoff · C-3는 **디렉터 Go/No-Go 후**만 |
| 2~5 | (동일) | `07` 6단계 |
| 6 | (2~4주 후) | `06` · C-3 Go 시 **shadow 4주** 별도 |

- C-3 **No-Go** → Handoff skip · `05`에 `"C-3 보류"` 기록

---

## Claude Pro 기존묶음 원문 (보존)

> 출처: `Desktop/클로드 프로/03_기존묶음C_작업지시서.md`

### 목표
반응형 순환매 추종 → **예측형 선취매** 고도화.

### 작업항목 (3개 — 원본 순서)

1. **크립토(비트겟) 리스크온/오프 선행신호** — 자본 격리, 신호만 KR/US `predictive_regime_ensemble` 6번째 팩터 후보 (BTC vol, funding 등 1~2개)
2. **섹터 순환매 예측** — `sector_rotation_store.py` Markov 1차→확장, `sector_spillover_refresh.py` 리드타임 실측
3. **데이터 신선도/유니버스** — `krx_list_survival`, `us_list_survival` 캐시 vs 선취매 병목

### 관련 핵심 파일
`predictive_regime_ensemble.py`, `sector_rotation_store.py`, `sector_spillover_refresh.py`, `cross_market_ssot.py`, `us_kr_theme_bridge.py`, `macro_sentinel_quant.py`

---

## 로컬 구현 현황 (2026-08-01)

| sub | 스펙 | 로컬 | 비고 |
|-----|------|------|------|
| C-1 | 섹터·스필오버 | ❌ 미착수 | Markov 1차·spillover lag 미실측 문서화 없음 |
| C-2 | 유니버스·캐시 | ❌ 미착수 | KR csv / US 300s·86400s TTL 현행 유지 |
| C-3 | bitget 6th factor | ❌ 미착수 / **No-Go 미결** | shadow 프로토콜 미시작 |

---

## Phase 목표 (한 줄)

반응형 순환매 → **예측형 선취매** (섹터·스필오버·데이터 신선도·[bitget 신호]).

---

## C-1 · 섹터 순환매 예측 정교화

**CAT**: C · **로컬**: ❌

### 스펙 (Claude Pro + 확장)

1. **`sector_rotation_store.py`**
   - Markov: **1차(직전만)** → **2차+** 확장 (Handoff: 전이 행렬 차수)
   - `PREDICTED_NEXT_SECTOR_{KR,US}` 갱신 주기·신뢰도 threshold
2. **`sector_spillover_refresh.py`**
   - US→KR 스필오버 **리드타임 실측** (lag 미확인 → diag 후 최적화)
   - synergy: `#순환매_선취매` / `[🌐스필로버 선취매]` (`scanner_synergy_engine.py`, CAT-C)

### Cursor 선독

`sector_rotation_store.py`, `sector_spillover_refresh.py`, `sector_rotation_smoothing.py`, `us_kr_theme_bridge.py`, `cross_market_ssot.py`, `scanner_synergy_engine.py`

### C-1 완료

- [ ] Markov 차수·smoothing
- [ ] spillover lag(일) config
- [ ] scanner synergy 태그 연동 확인
- [ ] 2~4주 lead time 관찰

---

## C-2 · 데이터 신선도 / 유니버스 품질

**CAT**: B + C · **로컬**: ❌

### 스펙 (Claude Pro 원문)

1. `krx_list_survival.py`, `us_list_survival.py` — 캐시 TTL vs **선취매 슬롯** 병목
2. `data_refresh` / US incremental vs 장중 scan **staleness** upper bound
3. 필요 시: TTL config_kv화, scan prelude universe refresh (CAT-A Handoff)

### 로컬 참고 (CAT-B)

- US list survival: 캐시 **300s / 86400s** tier
- KR: csv 기반 survival

### Cursor 선독

`krx_list_survival.py`, `us_list_survival.py`, `data_updater.py`, `factory_pipelines.py` (data_refresh step)

### C-2 완료

- [ ] 병목 결론 + TTL 변경
- [ ] staleness upper bound
- [ ] 2~4주 scan 지연 관찰

---

## C-3 · [선택] bitget 리스크온/오프 → KR/US 앙상블

**CAT**: G · **🔴 디렉터 Go/No-Go** · Claude Pro 원문 **작업 1번**

### 원칙 (헌법 + CAT-MAP)

- **자본 완전 격리** — bitget PnL/Kelly/forward **merge 금지**
- **신호만** — read-only adapter → `predictive_regime_ensemble` **6번째 팩터**

### Go일 때

1. bitget **1~2 지표** (BTC vol, funding rate 등)
2. adapter (`cross_market_ssot` 확장 또는 `crypto_leading_factor_bridge.py` 신규)
3. **shadow 4주** — `REGIME_ENSEMBLE` 기록만, Kelly/scan **미반영**
4. shadow hit rate ≥ threshold → 가중치 편입 (PRI cap 0.85, CAT-G)

### No-Go

- skip, 로그 `"C-3 보류"` → Phase 4 가능

### Claude Knowledge (Go 시)

- `@CAT-G` `@CAT-MAP` — bitget 코드 Project Knowledge **비권장**

### C-3 완료 (Go)

- [ ] adapter API + shadow config
- [ ] 6th factor name + weight cap
- [ ] 4주 shadow + hit rate

---

## 관련 파일 (전체)

```
sector_rotation_store.py, sector_spillover_refresh.py, sector_rotation_smoothing.py,
cross_market_ssot.py, us_kr_theme_bridge.py, macro_sentinel_quant.py,
predictive_regime_ensemble.py, krx_list_survival.py, us_list_survival.py,
scanner_synergy_engine.py, factory_pipelines.py
```

(bitget: Go 시만 — `bitget/` read-only, KR/US config write 금지)

---

## Phase 3 완료 시 05_진행로그 + 06

- Markov 차수 / spillover **lag(일)**
- universe cache **TTL**
- C-3: **Go/No-Go** + factor + shadow

---

## CAT 문서에 없어서 이 파일에만 있는 것

- Markov **2차+** · spillover **실측 의무**
- bitget **shadow 4주** · scanner_synergy **태그 체크리스트**
- **C-1/C-2/C-3 실행 순서** (bitget 후행) · **로컬 미착수 현황**

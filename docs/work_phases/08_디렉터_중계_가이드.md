# 디렉터 중계 가이드 — Claude Pro ↔ Cursor 연결

> **역할**: 디렉터(您) = **복붙·승인·Go/No-Go**만. 스펙 설계·코드는 AI가 한다.  
> **목적**: “뭐라고 말하지?” “뭘 붙이지?”를 **이 파일 한 장**으로 해결.  
> **연결고리 SSOT**: `05_진행로그.md` (항상 최신본을 양쪽에 공유)

---

## 한 장 흐름

```
[Claude Pro] Handoff 작성 ──복붙──→ [Cursor] 구현 + 05·00 갱신
                                        │
                    ←──복붙── 3줄 요약 + 05 해당 섹션 + (선택) diff
                                        │
[Claude Pro] OK / 수정 spec ──복붙──→ [Cursor] (수정 spec 있을 때만 재실행)
```

**규칙 3개**
1. Cursor 착수 **전** Claude Handoff **필수**
2. Claude **OK 전** sub-phase Done **금지**
3. 디렉터는 **요약을 새로 쓰지 말고** — Cursor/Claude가 쓴 **원문을 그대로** 넘긴다

---

## 지금 Claude Pro에 보낼 것 (A-3 검증 + A-1 재검증 대기)

Phase A 프로젝트 대화에 **아래 블록 전체**를 붙여넣으세요.  
Knowledge에 `05`, `00`, `07`, `01_묶음A`, `08`(본 파일)이 있으면 `@05_진행로그` 등으로 멘션.

---

### 📋 Claude Pro용 붙여넣기 — A-3 검증 요청 (2026-08-01)

```markdown
@05_진행로그 @00_전체현황판 @01_묶음A @08_디렉터_중계_가이드

## 요청
Cursor A-3 구현 결과를 Handoff·완료 기준과 대조해서 판정해줘.
**A-3 OK와 A-1 재검증을 분리**해서 답해줘.

---

## 1) SSOT / 호출 경로 (Claude 질문 1번에 대한 Cursor 답)

- `resolve_max_open_positions()` **정의**는 A-1부터 `performance_budget_governor.py` (SSOT).
- `forward/shared.py`는 try_add gate #6에서 **import + 호출만**. A-3 세션 `git diff forward/shared.py` = **변경 없음**.
- 로직 **중복 없음**. try_add 실제 경로:
  - 앞단: `is_block_new_entries()` (~1733) — LOCKDOWN block 플래그
  - gate #6: `resolve_max_open_positions(pre_sys_config, market)` (~2159)

---

## 2) A-1 읽기 버그 (Claude 질문 2번 — A-3 범위 밖, A-1 재검증)

- **미변경**: `evaluate_performance_budget`, tier 계산, config_kv **쓰기**
- **변경**: `resolve_position_quota_mult()` **읽기**만
  - 수정 전: `float(cfg.get(key, 1.0) or 1.0)` → `0.0`이 `1.0`으로 오독
  - 수정 후: 키 있으면 `float(raw)` → `0.0` 유지
- **실전 함의**:
  - `is_block_new_entries` (bool 플래그) 경로는 버그와 **무관** → LOCKDOWN **주 차단**은 살아 있었을 가능성
  - gate #6 `mult=0` 경로는 **깨져 있었음** (쿼터 0이 아닌 ~20 허용)
- **테스트** `test_lockdown_mult_zero_no_min_one`: **수정 후** 통과 (수정 전엔 실패)
- **판정 요청**: A-1 「구현됨」→ **재검증 큐**로 보는 게 맞는지 + A-1 재검증 Handoff 필요 여부

---

## 3) 05_진행로그 A-3 섹션 원문 (Cursor가 직접 기록)

(05_진행로그.md의 「### A-3 국면 쿼터」~「### A-1 부수 발견」까지 전문을 여기 붙여넣기)

---

## 4) 테스트

`pytest tests/test_performance_budget_regime_quota_a3.py` — 12 passed

---

## Claude에게 원하는 답 형식

### A-3
- [ ] OK / [ ] 수정 spec (항목별)
- SSOT·곱연산·국면 read·테스트 — Handoff 완료 기준 체크리스트

### A-1 (재검증)
- [ ] 읽기 버그 수정 **승인** / [ ] 롤백·별도 패치 요청
- LOCKDOWN 2중 게이트(block vs mult=0) 정책 확인
- A-1 Claude OK 가능 여부 (아니면 추가 Handoff 항목)

### 다음 sub-phase
- OK 시: A-4 Handoff 작성할지 / A-1 재검증 Handoff 먼저 할지 **순서 제안**
```

> **디렉터 작업**: 아래 「부록 A」05 원문이 최신이면 그대로 Claude에 전송. 아니면 `05_진행로그.md`에서 A-3·A-1 부수 발견 섹션을 복사해 「3)」 자리에 넣기.

---

## 부록 A — 05_진행로그 A-3·A-1 부수 발견 (2026-08-01 원문)

```markdown
### A-3 국면 쿼터 — [2026-08-01] · 구현 1단계 ✅ / Claude OK ❌ / 효과 3단계 ❌

- **SSOT 위치 (중요)**: `resolve_max_open_positions()` 정의는 **A-1부터** `performance_budget_governor.py`. `forward/shared.py`는 try_add gate #6에서 **import·호출만** (A-3 세션에서 shared.py **diff 없음**). 로직 중복 없음.
- **변경 요약**: `performance_budget_governor.resolve_max_open_positions` — `regime_base`(POSITION_QUOTA_REGIME_MAP) × `POSITION_QUOTA_MULT_{market}` **곱연산(floor)**. `POSITION_QUOTA_REGIME_MAP` SSOT 등록.
- **POSITION_QUOTA_REGIME_MAP** (config_kv + `performance_budget_governor.py`):
  - BULL=20, SIDEWAYS=15, HIGH_VOL=10, BEAR=8, DEFAULT=20
- **결합 방식**: **곱(mult)** — `floor(regime_base × POSITION_QUOTA_MULT_{KR|US})`; mult>0 & result<1 → 1; mult≤0 → 0 (min-1 미적용)
- **국면 read**: `{KR|US}_REGIME_KEY` → `REGIME_ENSEMBLE.markets.{KR|US}.regime` (ensemble 기록 재사용, 판별 로직 추가 없음)
- **신규/수정 파일**: `performance_budget_governor.py`, `tests/test_performance_budget_regime_quota_a3.py`
- **테스트 (A-3 범위)**: 12 passed — **수정 후 코드** 기준 (`pytest tests/test_performance_budget_regime_quota_a3.py`)
- **잔여**: [ ] Claude Pro OK · [ ] 디렉터 Critical 승인 · [ ] 2~4주 `06` 효과 검증

### A-1 부수 발견 · POSITION_QUOTA_MULT **읽기** 버그 — [2026-08-01, A-3 세션] · **A-1 재검증 대기**

- **범위**: A-3 Handoff **밖**. `evaluate_performance_budget` / tier **계산·config_kv 쓰기(A-1)** 는 **미변경**.
- **위치**: `performance_budget_governor.resolve_position_quota_mult()` (읽기 전용 헬퍼)
- **수정 전**: `float(cfg.get(key, 1.0) or 1.0)` → 키가 `0.0`이면 Python falsy로 **`1.0`으로 오독**
- **수정 후**: 키 존재 시 `float(raw)` 그대로 → `0.0` 유지
- **실전 함의**:
  - LOCKDOWN tier는 `block_new_entries=True` + `is_block_new_entries()` **선행 게이트**(try_add ~1733)가 **별도** 동작 → 신규 차단 **주 경로는 살아 있었을 가능성**
  - gate #6 `resolve_max_open_positions(mult=0)` 경로는 **깨져 있었음** (0.0→1.0 오독 → base×1 ≈ 20 OPEN 허용)
- **상태**: A-1 「구현됨」→ **Claude OK 보류** (본 읽기 버그 재검증 필요)
- **테스트**: `test_lockdown_mult_zero_no_min_one` — **수정 후** 통과
```

---

## 부록 C — A-1-R1 Kelly throttle 확인 (Claude 우려 대응 · 2026-08-01)

Claude: 「`_resolve_performance_budget_mult`(A-2) 확인 안 됨」→ **A-1-R1에서 이미 수정·테스트 완료**. 아래를 Claude에 붙여넣기.

```markdown
@05_진행로그 A-1-R1 섹션 @00_전체현황판

## A-1-R1 추가 확인 — Kelly throttle (A-2) 전수 감사 결과

Claude가 우려한 `_resolve_performance_budget_mult` (KELLY_THROTTLE_MULT_KR/US):

**수정 전** (동일 falsy 버그 있었음 — MDD 헌법급):
`float(sys_config.get(f"KELLY_THROTTLE_MULT_{mkt}", 1.0) or 1.0)`

**수정 후** (`meta_governor_consumer.py` L136-143):
→ `resolve_kelly_throttle_mult()` → `resolve_config_float()` (0.0 유지)

**회귀 테스트** (`tests/test_a1_r1_lockdown_mult_read.py`):
- `test_resolve_performance_budget_mult_zero_not_misread` ✅
- `test_lockdown_kelly_mult_zero_step4_return` — Step4 return = **0.0** ✅

**PERFORMANCE_BUDGET 읽기 chain 전수 (05 표 #1~#4)**: Kelly + Quota + bool gate — **R1 범위 내 전부 확인**.

**LOCKDOWN defense-in-depth**: 00 SSOT에 「삭제/통합 금지」 rationale 추가됨.

## 판정 요청
- A-1-R1: OK (Kelly+Quota read path)
- A-1 본체: R1 OK 후 Claude OK 가능 여부
- A-3: 별도 OK 유지
```

---

```
1. 변경: performance_budget_governor.py — POSITION_QUOTA_REGIME_MAP + resolve_max_open_positions(국면 base × mult, floor). forward/shared.py diff 없음(호출만).
2. config: POSITION_QUOTA_REGIME_MAP (BULL 20 / SIDEWAYS 15 / HIGH_VOL 10 / BEAR 8 / DEFAULT 20).
3. 테스트: pytest tests/test_performance_budget_regime_quota_a3.py 12 passed. A-1 읽기 버그(mult=0→or 1.0) 별도 재검증 대기.
```

---

## Claude Pro가 돌려줘야 하는 답 (기대 형식)

Claude가 아래 형식으로 답하면 Cursor에 그대로 넘기기 쉽습니다.

```markdown
## A-3 판정: OK | 수정 spec
- (체크리스트 항목별 한 줄)

## A-1 재검증 판정: 승인 | 추가 Handoff 필요
- (읽기 버그·LOCKDOWN 2중 게이트 결론)

## 05에 추가할 한 줄
- A-3: Claude OK: YYYY-MM-DD  (또는: 수정 spec → Cursor 재실행)

## 다음 Handoff
- sub-phase ID: A-4 | A-1-R1(재검증)
- (Handoff 본문 또는 "아래 Handoff Cursor에 붙여넣기")
```

**디렉터**: Claude OK 한 줄을 `05_진행로그.md` 해당 sub에 **직접 추가** (Claude가 05를 못 고치므로).

---

## 상황별 — Claude Pro에 뭐라고 말하면 되나

### ① 처음 sub-phase 시작 (Handoff 받기)

```text
@07_듀얼AI_협업루프 @01_묶음A @05_진행로그 @00_전체현황판

05 최신 기준 다음 sub-phase Handoff 작성해줘.
코드 쓰지 마. CAT-HANDOFF 형식. sub-phase 하나만.
```

### ② Cursor 구현 끝 — 검증 요청

```text
@05_진행로그 @08_디렉터_중계_가이드

sub-phase {A-3} Cursor 구현 완료. 검증해줘.

[Cursor 3줄 요약 그대로 붙여넣기]

[05 해당 sub-phase 섹션 전문 붙여넣기]

OK / 수정 spec. 05에 넣을 "Claude OK" 한 줄도 알려줘.
```

### ③ Claude가 수정 spec 줬을 때 — Cursor에

```text
아래 Claude 수정 spec 반영. sub-phase {A-3}만. targeted diff.
세션 종료 시 05·00 갱신 + 3줄 요약.

[Claude 수정 spec 전문]
```

### ④ 충돌 — Cursor가 Adapter 제안했을 때

```text
@CAT-HANDOFF

Cursor 충돌 보고야. spec 수정 vs Adapter 중 뭐가 맞는지 판정해줘.

[Cursor 충돌 보고 전문]
```

### ⑤ 🔴 Critical — 디렉터 승인 문구 포함 요청

```text
🔴 Critical sub-phase {A-3}. Handoff에 디렉터 승인 체크리스트 포함해서 작성해줘.
```

---

## Cursor에게 뭐라고 말하면 되나

### Handoff 받아서 구현 시작

```text
아래 Claude Handoff 그대로 구현만. sub-phase: {A-x}.
targeted diff only. 세션 종료 전 05_진행로그.md + 00_전체현황판.md 갱신 + 3줄 요약.

[Handoff 전문]
```

### Claude OK 받은 뒤 — 05만 갱신 (코드 변경 없을 때)

```text
05_진행로그 {A-x} 섹션에 "Claude OK: {날짜}" 한 줄 추가해줘. 코드 변경 없음.
```

---

## 붙여넣기 체크리스트 (디렉터용)

| 단계 | Claude Pro에 | Cursor에 |
|------|-------------|----------|
| Handoff 요청 | `07`, `01~04`, `05`, `00` | — |
| 구현 지시 | — | Handoff **전문** + 05·00 갱신 지시 |
| 검증 요청 | `05` **해당 섹션 전문** + Cursor 3줄 요약 | — |
| 수정 반영 | — | Claude **수정 spec 전문** |
| OK 기록 | Claude가 준 OK 한 줄 | 또는 Cursor에 05 갱신 요청 |

**하지 말 것**
- 디렉터가 임의로 요약 재작성 (정확도 ↓)
- Claude OK 없이 “다음 sub-phase” Cursor 실행
- 한 메시지에 sub-phase 2개

---

## 현재 대기열 (2026-08-01 갱신)

| sub | Cursor | Claude | 디렉터 | 다음 |
|-----|--------|--------|--------|------|
| A-1 | 구현됨 ⚠️ | **OK 보류** (읽기 버그) | — | Claude A-1 재검증 판정 |
| A-2 | 구현됨 | OK ❌ | — | A-3/A-1 정리 후 검증 |
| A-3 | 구현됨 | **OK 대기** | Critical 승인 대기 | **↑ 위 붙여넣기 블록 전송** |
| A-4~ | 미착수 | — | — | A-3 OK + A-1 정리 후 |

---

## 파일 역할 (입 맞추기)

| 파일 | Claude | Cursor | 디렉터 |
|------|--------|--------|--------|
| `07_듀얼AI_협업루프.md` | 6단계 규칙 | 세션 종료 의무 | 전체 흐름 |
| **`08_디렉터_중계_가이드.md`** | 검증 요청 형식 | 구현 지시 형식 | **복붙 템플릿** |
| `05_진행로그.md` | 판정 입력 | **매 세션 갱신** | OK 한 줄 추가 |
| `00_전체현황판.md` | SSOT·Phase 표 | **매 세션 갱신** | 스냅샷 확인 |
| `CAT-HANDOFF_템플릿.md` | Handoff 출력 형식 | 수신 spec 형식 | — |

---

## Claude Pro Knowledge 권장 (중계용)

Phase A 프로젝트에 **반드시** 포함:
- `08_디렉터_중계_가이드.md` ← **본 파일**
- `07`, `05`, `00`, `01_묶음A`, `06`
- `docs/claude_project/CAT-HANDOFF_템플릿.md`

Custom Instructions 한 줄:
> 코드 작성 금지. Handoff·검증·OK/수정 spec만. 디렉터가 붙여넣은 05 **원문**과 Handoff를 대조할 것.

---

## Cursor 새창 시작 멘트 (SSOT)

> **갱신**: 2026-08-09  
> **용도**: Track A / Track B **Cursor 새 창** 열 때 **첫 메시지로 복붙**. 채팅에 긴 spec 쓰지 말고 **파일이 spec**.  
> **연결**: `10_작업함_프로토콜.md` · `independent_verification/`

### 공통 원칙

| 순서 | 읽을 파일 | 역할 |
|------|-----------|------|
| 1 | `NEXT_ACTION.md` | 지금 누가 · 뭘 · status |
| 2 | `CLAUDE_TO_CURSOR.md` | **구현 spec (유일)** |
| 3 | (V-* 시) `independent_verification/` 동일 2파일 | IV spec |
| **종료** | `05` · `00` · `CURSOR_TO_CLAUDE` · `NEXT_ACTION` | Claude 다음 라운드 |

**금지**: Handoff를 채팅에 길게 쓰기 · Handoff 없이 구현 · sub-phase 2개 · 트랙 혼합

---

### Track A (KR/US) — 창 1

**워크스페이스**: 레포 루트 `Dual-Screener-Bot` · **`bitget/` 수정 금지**

#### A-1 구현 (가장 자주)

```
Track A (KR/US) 구현 세션.

1) docs/work_phases/NEXT_ACTION.md — status가 WAIT_CURSOR_IMPL인지 확인. 아니면 멈추고 보고.
2) docs/work_phases/CLAUDE_TO_CURSOR.md = 유일한 spec. 채팅 지시 무시.
3) .cursorrules 준수: sub-phase 1개 · targeted diff only · bitget/ 수정 금지.
4) 충돌 시 Adapter 제안 후 Ask. 🔴 F/G/N/B 방어계층은 보수적으로.

세션 종료 전 필수:
- docs/work_phases/05_진행로그.md (해당 sub 섹션)
- docs/work_phases/00_전체현황판.md
- docs/work_phases/CURSOR_TO_CLAUDE.md (검증 OUTBOX)
- docs/work_phases/NEXT_ACTION.md → status: WAIT_CLAUDE_OK
- 디렉터용 3줄 요약

지금 시작해.
```

#### A-2 독립검증 V-*

```
Track A — Independent Verification (V-*) 구현 세션.

1) docs/independent_verification/NEXT_ACTION.md
2) docs/independent_verification/CLAUDE_TO_CURSOR.md = 유일한 spec
3) docs/independent_verification/01_자기채점_위험_헌법.md — L0/L1/L2/L3 위반 없이
4) RP-1·백테스트로 LIVE 승격 연동 금지. V-1은 WARN만, BLOCK 기본 OFF.

종료 시:
- independent_verification/CURSOR_TO_CLAUDE.md
- independent_verification/NEXT_ACTION.md
- docs/work_phases/05_진행로그.md IV 섹션
- 3줄 요약

시작해.
```

#### A-3 배포·관측만 (구현 X)

```
Track A — 배포/관측만. 새 기능 구현 금지.

docs/work_phases/NEXT_ACTION.md 읽고 디렉터 할 일만 수행:
- deploy_watch / factory.sh / VPS 확인
- F-GATE / F-RETIRE / L-OBS 관련

코드 변경 필요하면 멈추고 Handoff 요청만 보고.
결과는 CURSOR_TO_CLAUDE.md에 짧게 기록.
```

#### A-4 조사·Reality Audit (구현 X)

```
Track A — 조사만. 구현·리팩터 금지.

docs/work_phases/CURSOR_TO_CLAUDE.md 최신 OUTBOX 기준으로
코드·DB read-only 실측만. SQL/쿼리 결과 정리.

산출: CURSOR_TO_CLAUDE.md 해당 섹션 갱신 + 05 한 줄. 코드 diff 없음.
```

#### A-5 상태 점검 (뭐 할지 모를 때)

```
Track A — 상태 점검만. 구현 시작하지 마.

읽기 순서:
1) docs/work_phases/NEXT_ACTION.md
2) docs/work_phases/CLAUDE_TO_CURSOR.md (Handoff 있나)
3) docs/work_phases/CURSOR_TO_CLAUDE.md (미검증 OUTBOX 있나)
4) docs/independent_verification/NEXT_ACTION.md

출력:
- 지금 status / 다음 담당(디렉터·Claude·Cursor)
- 구현 가능하면 sub-phase ID 1개만
- 없으면 "Claude Handoff 대기" 명시

코드 수정 금지.
```

---

### Track B (Bitget) — 창 2

**범위**: `bitget/` 만 · **루트 KR/US 모듈 수정 금지**

#### B-1 구현

```
Track B (Bitget) 구현 세션.

1) bitget/docs/work_phases/NEXT_ACTION.md — status 확인
2) bitget/docs/work_phases/CLAUDE_TO_CURSOR.md = 유일한 spec
3) bitget/ 하위만 수정. 루트 KR/US 모듈 import·수정 금지 (CAT-MAP).
4) sub-phase 1개 · targeted diff only.

종료 시:
- bitget/docs/work_phases/05_진행로그.md
- bitget/docs/work_phases/00_전체현황판.md
- bitget/docs/work_phases/CURSOR_TO_CLAUDE.md
- bitget/docs/work_phases/NEXT_ACTION.md → WAIT_CLAUDE_OK
- 3줄 요약

시작해.
```

#### B-2 shadow·관측 (production 변경 금지)

```
Track B — shadow/관측 세션. production Kelly·mult 변경 금지.

bitget/docs/work_phases/NEXT_ACTION.md + walk_forward_shadow 관련만.
docs/independent_verification/02_편향_체크리스트 — IV-04, IV-25 참고.
WALK_FORWARD_PROMOTION_BLOCK_ENABLED 켜지 마.

로그·테이블 read + 보고만. 변경 시 shadow 격리 테스트 필수.
```

#### B-3 상태 점검

```
Track B — 상태 점검만. 구현 금지.

1) bitget/docs/work_phases/NEXT_ACTION.md
2) bitget/docs/work_phases/CLAUDE_TO_CURSOR.md
3) docs/independent_verification/03_시장별_현황_매트릭스.md (BG 열)

다음 할 일 1개 + R3 paper 미검증 여부 보고. 코드 수정 금지.
```

---

### Claude 한 줄 (Cursor 세션 끝난 뒤)

| 트랙 | 붙여넣기 |
|------|----------|
| Track A | `docs/work_phases/CURSOR_TO_CLAUDE.md 검증. OK면 CLAUDE_TO_CURSOR.md에 다음 Handoff. 채팅 말고 파일에.` |
| Track B | `bitget/docs/work_phases/CURSOR_TO_CLAUDE.md 검증. OK면 bitget CLAUDE_TO_CURSOR에 Handoff. 채팅 말고 파일에.` |
| IV | `docs/independent_verification/CURSOR_TO_CLAUDE.md 검증. OK면 IV CLAUDE_TO_CURSOR에 V-* Handoff.` |

---

### 쓰지 말 것

| 나쁜 시작 | 이유 |
|-----------|------|
| "백테스트 잘 되게 고쳐줘" | L0/L2 층 없음 → 자기채점 |
| "전체 점검하고 다 고쳐줘" | sub-phase 1개 규칙 위반 |
| "Claude가 말한 대로: …" (긴 복붙) | `CLAUDE_TO_CURSOR.md`와 불일치 |
| Track A 창에서 bitget 작업 | 트랙 오염 |
| Handoff 없이 "다음 단계 구현" | `10_작업함` 위반 |

---

### 새창 선택 흐름

```
새 Cursor 창
  → NEXT_ACTION status 확인
  → WAIT_CURSOR_IMPL  → A-1 / B-1
  → VPS·배포만        → A-3
  → 조사만            → A-4
  → V-*               → A-2
  → IV 관측 리포트    → A-6 (텔레그램 ---CURSOR--- 붙여넣기)
  → 모름              → A-5 / B-3
  → 세션 끝           → CURSOR_TO_CLAUDE + NEXT_ACTION
  → Claude 한 줄 검증
```

#### A-6 IV 관측 리포트 (텔레그램 받은 뒤)

```
텔레그램 [IV_OBS] 메시지에서 ---CURSOR--- 아래 블록 전체를 복사해 첫 메시지로 붙여넣기.

또는 수동 실행 후:
factory data/iv_observation_latest.json 의 cursor_prompt 필드 사용.

구현·V-2 BLOCK 켜기는 readiness=READY 일 때만. 그 전에는 리뷰·요약만.
```

*Bitget 대칭: `bitget/docs/work_phases/08_디렉터_중계_가이드.md` → 본 섹션 참조.*

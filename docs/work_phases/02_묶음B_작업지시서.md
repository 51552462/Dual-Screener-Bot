# Phase 2 — 묶음B: 진화엔진/면역/전략생애주기

> **선행**: Phase 1 **A-5까지** + `05_진행로그.md` 전체 · (현재 A-5 미완 — **B 시작 전 A 잔여 확인**)  
> **범위**: KR/US · 관련 파일 약 **52개**  
> **CAT 매핑**: CAT-H(주), CAT-F(deathmatch·lifecycle), CAT-I(참고)  
> **Claude Knowledge**: `@CAT-H` `@CAT-F` `@CAT-MAP` `@CAT-CONSTANTS` `@CAT-KR-US`  
> **통합본**: Claude Pro `02_기존묶음B` + Cursor sub-phase + 로컬 대조 (2026-08-01)

---

## ⚠️ CAT 문서와의 관계

`docs/claude_project/CAT-H_진화HTC.md` — HTC·OOS·INCUBATOR.  
이 파일 — **유전자풀·마찰·데스매치 킬·S5 생애주기** Phase 2 전용.

**실행 순서**: `B-1 → B-2 → B-3 → B-4` (B-1/B-2는 격리 가능하나 **로드맵상 A 완료 후 권장**)

| 위험도 | CAT | 승인 |
|--------|-----|------|
| 🟡 High | H, F | Claude spec + 디렉터 (deathmatch alloc) |
| 🟢 Medium | I (면역) | 파일 목록 참고만 — **별도 작업항목 없음** (아래 참조) |

---

## 듀얼 AI 협업 루프 (필수 · 본 Phase)

> **전문 SSOT**: `07_듀얼AI_협업루프.md` · **본 Phase sub-phase**: `B-1` ~ `B-4`

| 단계 | 담당 | 행동 |
|------|------|------|
| 1 | **Claude Pro** | `B-x` Handoff 작성 · Phase1 `05`의 S5/arm 이름 **선행 확인** |
| 2 | **디렉터** | Handoff → Cursor |
| 3 | **Cursor** | 구현 · **`05` + `00` 갱신** · 3줄 요약 |
| 4 | **디렉터** | → Claude 검증 요청 |
| 5 | **Claude Pro** | OK / 수정 spec |
| 6 | (2~4주 후) | `06` 효과 검증 |

- **B 시작 전**: Phase1 A-5 완료 + Claude OK 권장 (현재 A-5 미완 — `05` 확인)

---

## Claude Pro 기존묶음 원문 (보존)

> 출처: `Desktop/클로드 프로/02_기존묶음B_작업지시서.md`  
> 시작 전 `05_진행로그` Phase1 결과 붙여넣기 (방어arm 이름 등)

### 목표
"자율진화"가 실제로 **다양한 알파**를 찾고, **실전 마찰**을 반영하며, **선제적 전략 킬**.

### 작업항목 (4개)

1. **유전자풀 확장** — mean-reversion / vol structure / corr 계열 변수 추가
2. **GP 보상함수 마찰** — 슬리피지·회전율 페널티, `synthetic_data_generator` 노이즈 검토
3. **데스매치 선제적 킬** — lookback 90~120d → 45d 또는 실시간 MDD -8% STANDBY
4. **S5 전략 생애주기** — Phase1 S5가 `strategy_promotion_engine` OBSERVING→LIVE 정상 편입

### 관련 핵심 파일
`genetic_expr_builder.py`, `incubator_engine.py`, `synthetic_data_generator.py`, `evolution/deathmatch_scorecard.py`, `evolution/deathmatch_allocation.py`, `strategy_promotion_engine.py`, `toxic_decay_bandit.py`, `clustered_immune_vaccine.py`

---

## 로컬 구현 현황 (2026-08-01)

| sub | 스펙 | 로컬 | 비고 |
|-----|------|------|------|
| B-1 | 유전자풀 확장 | ❌ 미착수 | `PRICE_VARS` 7개, `VOL_VARS` 3개만 |
| B-2 | GP 마찰 | ❌ 미착수 | turnover/slip λ 없음 |
| B-3 | 데스매치 킬 | ❌ 미착수 | lookback KR **90d**, US **120d** (`deathmatch_config.py`) |
| B-4 | S5 lifecycle | ❌ 미착수 | A-5 선행 |

### 면역 파일 (`toxic_decay_bandit`, `clustered_immune_vaccine`)

Claude Pro **관련 파일**에만 있고 **작업항목 없음**. CAT-I 참고·향후 별도 Phase 또는 B 서브태스크로 Handoff 시 추가. **본 로드맵 B-1~B-4에 포함하지 않음** (누락 아님 — 원본 스펙 동일).

---

## Phase 목표 (한 줄)

자율진화가 **다양한 알파** + **실전 마찰** + **손실 전략 선제 킬**.

---

## B-1 · 유전자풀 확장

**CAT**: H · **SSOT**: `genetic_expr_builder.py` · **로컬**: ❌

### 스펙 (Claude Pro 원문)

| 계열 | 추가 변수 (예시) |
|------|-----------------|
| 평균회귀 | zscore(close, ma20), bollinger_pct_b, rsi_divergence |
| 변동성구조 | realized_vol_ratio(단/장), atr_regime |
| 상관 (옵션) | sector_relative_strength, cross_asset_corr |

### 로컬 현행

```python
PRICE_VARS = ("open", "high", "low", "close", "ma5", "ma10", "ma20")
VOL_VARS = ("volume", "vol_ma5", "vol_lag1")
```

### 설계 주의

- `is_safe_expression` AST 화이트리스트 통과만 (CAT-H)
- gear/panic shift **유지** — 풀만 확장

### Cursor 선독

`genetic_expr_builder.py`, `incubator_engine.py` (`build_local_vars`)

### B-1 완료 (3단계)

- [ ] 변수 목록 + local_vars wiring
- [ ] cold-start population smoke
- [ ] 2~4주 GP 다양성 관찰

---

## B-2 · GP 보상함수 + 합성 마찰

**CAT**: H · **로컬**: ❌

### 스펙

1. `incubator_engine.evaluate_mutant_strategies`: 슬리피지·**고회전** fitness 페널티
2. `synthetic_data_generator.py`: 스프레드/슬리피지 노이즈 — Handoff 채택/보류

### 수식 (Handoff 확정)

```
fitness_adj = sharpe (or rank) - λ_turnover × turnover - λ_slip × estimated_slip
```

### B-2 완료

- [ ] λ config 키
- [ ] synthetic noise on/off
- [ ] 2~4주 고회전 전략 감소 관찰

---

## B-3 · 데스매치 선제적 킬

**CAT**: F · **선행**: A-2+ · **로컬**: ❌

### 스펙 (Claude Pro 원문 + 옵션)

| 옵션 | 내용 |
|------|------|
| A | lookback 90/120d → **45d** |
| B | 실시간 MDD ≤ **-8%** → lookback 없이 STANDBY |
| A+B | 단축 lookback + 실시간 킬 |

### 로컬 현행 (`evolution/deathmatch_config.py`)

| 시장 | lookback_days |
|------|---------------|
| KR | **90** |
| US | **120** |

- `META_GROUP_KELLY_MULT ×0` on STANDBY (CAT-F)
- Gate: `DEATHMATCH_APPLY_ALLOCATION=1`

### Cursor 선독

`evolution/deathmatch_scorecard.py`, `deathmatch_allocation.py`, `deathmatch_battle_royale.py`, `deathmatch_config.py`

### B-3 완료

- [ ] 킬 트리거 config + 임계값
- [ ] STANDBY → Kelly mult 0
- [ ] 2~4주 조기 킬 관측

---

## B-4 · S5 전략 생애주기 편입

**CAT**: F · **선행**: Phase 1 **A-5** · **로컬**: ❌

### 스펙

- `strategy_promotion_engine.py`: S5 / inverse / blackhole → `OBSERVING→CANDIDATE→LIVE→COOLED→RETIRED`
- Phase 1 **arm 이름·group_key** registry 일치
- Fast-track prefix S5 해당 여부

### KR/US lifecycle (CAT-CONSTANTS)

| | KR | US |
|---|----|----|
| min trades | 15 | 8 |
| alpha_half_life | 10d | 30d |

### B-4 완료

- [ ] S5 registry entry 예시
- [ ] CANDIDATE→LIVE gate 테스트
- [ ] → Phase 3(C) 시작 가능

---

## 관련 파일 (전체)

```
genetic_expr_builder.py, incubator_engine.py, synthetic_data_generator.py,
evolution/deathmatch_scorecard.py, evolution/deathmatch_allocation.py,
evolution/deathmatch_battle_royale.py, evolution/deathmatch_config.py,
strategy_promotion_engine.py, toxic_decay_bandit.py, clustered_immune_vaccine.py
```

---

## Phase 2 완료 시 05_진행로그 + 06 표

- 추가 **유전자 변수**
- fitness **λ** + synthetic slippage on/off
- 데스매치 lookback / **-8% MDD 킬**
- S5 **group_key** + promotion 결과

---

## CAT 문서에 없어서 이 파일에만 있는 것

- 유전자풀 **3계열** 명시 · GP **turnover/slip** 수식
- 데스매치 **45d + -8% STANDBY** · S5 ↔ A-5 **의존**
- **로컬 lookback 90/120 현행값** · 면역 파일 **미포함 사유**

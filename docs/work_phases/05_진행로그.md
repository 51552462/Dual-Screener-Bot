# 진행 로그

> 각 **sub-phase** 완료 시 추가. 다음 창 시작 시 **이 파일 전체** 붙여넣기.  
> sub-phase ID: A-1~A-5, B-1~B-4, C-1~C-3, D-1~D-3  
> **3단계 완료** 기준: `06_검증체크리스트_및_실패기록.md`  
> **협업 루프**: `07_듀얼AI_협업루프.md`

---

## 갱신 규칙

| 누가 | 언제 | 무엇 |
|------|------|------|
| **Cursor** | sub-phase 구현 세션 **종료 전** | 해당 sub 섹션 (날짜·요약·config·파일·잔여) |
| **Claude Pro** | OK 사인 후 | 해당 sub에 **`Claude OK: YYYY-MM-DD`** 한 줄 |
| **디렉터** | 2~4주 후 | `06` 효과 검증 표 |

**Claude OK 없이 sub-phase Done 처리 금지.**

---

## Phase 1 · 묶음A

### A-1 성과예산 거버너 — [2026-07-30] · 구현 1단계 ⚠️ / Claude OK ❌ / 효과 3단계 ❌

> **2026-08-01 A-1-R1**: falsy 0.0 읽기 버그 **Kelly+Quota 통합 수정** — Claude OK **R1 후** 판정

- **변경 요약**: `performance_budget_governor.py` 신설. NAV/HWM fresh read → 소진율 tier → config_kv SSOT. `meta_governor._step_treasury()`에서 `sync_performance_budget_to_config_kv()` 호출.
- **config_kv 키**:
  - `PERFORMANCE_BUDGET_MDD_CAP_PCT`, `ENABLE_PERFORMANCE_BUDGET_GOVERNOR`
  - `PERFORMANCE_BUDGET_STATE_KR`, `PERFORMANCE_BUDGET_STATE_US`
  - `KELLY_THROTTLE_MULT`, `KELLY_THROTTLE_MULT_KR`, `KELLY_THROTTLE_MULT_US`
  - `POSITION_QUOTA_MULT_KR`, `POSITION_QUOTA_MULT_US`
  - `PERFORMANCE_BUDGET_DEFENSE_ARM_ACTIVE_KR`, `_US`
  - `PERFORMANCE_BUDGET_BLOCK_NEW_ENTRIES_KR`, `_US`
- **tier 임계값**: 40% / 70% / 90% (band: NORMAL / CAUTION / DEFENSE / LOCKDOWN)
- **로컬 설계 주의 (코드 반영됨)**:
  - `treasury_state.json`의 `mdd_pct`(역대 최대) **재사용 금지** — 매 호출 nav/hwm으로 current_dd 재계산
  - `ENABLE_KELLY_NAV_DD_OVERLAY=False` — Ch.4 elasticity NAV축 비활성, 성과예산 거버너 일원화
- **신규/수정 파일**: `performance_budget_governor.py`(신규), `meta_governor.py`
- **잔여 (A-1)**:
  - [ ] 단독 dry-run·단위 테스트 문서화
  - [ ] 2~4주 가상매매 효과 검증 (`06` 표 기록)

### A-1 2차 패치 — [2026-07-30]

- **변경 요약**:
  1. Kelly throttle **클램프 순서** 수정
  2. config_kv **개별 키** 쓰기로 전환
  3. NAV 드로다운 **역대최대 고정 버그** 수정
  4. `ENABLE_KELLY_NAV_DD_OVERLAY=False` 일원화 (패치 D)
  5. 킬스위치 OFF 시 **잔여 LOCKDOWN** 플래그 해제 (`is_block_new_entries`)
- **신규/수정 파일**: `performance_budget_governor.py`, `meta_governor_consumer.py`, `kelly_elasticity_overlay.py`(패치 E)

### A-2 Kelly throttle — [2026-07-30] · 구현 1단계 ✅ / 효과 3단계 ❌

- **merge chain 삽입 위치**: `meta_governor_consumer.apply_meta_kelly_merge()` **return 직전** × `_resolve_performance_budget_mult(sys_config, entry_facts)` — Step4 merge **이후** 곱연산 (CAT-F chain Step4 끝)
- **시장 키**: `KELLY_THROTTLE_MULT_{KR|US}` 우선, 없으면 합성 `KELLY_THROTTLE_MULT`
- **신규/수정 파일**: `meta_governor_consumer.py`
- **잔여 (A-2)**:
  - [x] throttle=0 → `apply_meta_kelly_merge` 0.0 + `is_block_new_entries` 게이트 단위 테스트 (`tests/test_a2_kelly_throttle.py`)
  - [x] NORMAL tier Kelly regression 테스트 (meta=None / meta=BULL, throttle=1.0)
  - [ ] try_add E2E (full mock) — defer · gate #1 `is_block_new_entries`가 LOCKDOWN 주 경로
  - [ ] 2~4주 효과 검증

### A-3 국면 쿼터 — [2026-08-01] · 구현 1단계 ✅ / **Claude OK: 2026-08-05** / 효과 3단계 ❌

- **SSOT 위치 (중요)**: `resolve_max_open_positions()` 정의는 **A-1부터** `performance_budget_governor.py`. `forward/shared.py`는 try_add gate #6에서 **import·호출만** (A-3 세션에서 shared.py **diff 없음**). 로직 중복 없음.
- **변경 요약**: `performance_budget_governor.resolve_max_open_positions` — `regime_base`(POSITION_QUOTA_REGIME_MAP) × `POSITION_QUOTA_MULT_{market}` **곱연산(floor)**. `POSITION_QUOTA_REGIME_MAP` SSOT 등록.
- **POSITION_QUOTA_REGIME_MAP** (config_kv + `performance_budget_governor.py`):
  - BULL=20, SIDEWAYS=15, HIGH_VOL=10, BEAR=8, DEFAULT=20
- **결합 방식**: **곱(mult)** — `floor(regime_base × POSITION_QUOTA_MULT_{KR|US})`; mult>0 & result<1 → 1; mult≤0 → 0 (min-1 미적용) · **provenance: Claude Pro A-3 Handoff 명시** (`곱연산 mult 확정, min 아님` — Cursor 자체 판단 아님)
- **국면 read**: `{KR|US}_REGIME_KEY` → `REGIME_ENSEMBLE.markets.{KR|US}.regime` (ensemble 기록 재사용, 판별 로직 추가 없음)
- **신규/수정 파일**: `performance_budget_governor.py`, `tests/test_performance_budget_regime_quota_a3.py`
- **테스트 (A-3 범위)**: 12 passed — **수정 후 코드** 기준 (`pytest tests/test_performance_budget_regime_quota_a3.py`)
- **잔여**: [x] Claude Pro OK (2026-08-05) · [ ] 디렉터 Critical 승인 · [ ] 2~4주 `06` 효과 검증 · [ ] A-3b 경계 통합 테스트(선택)

### A-1-R1 재검증 · falsy 0.0 읽기 버그 — [2026-08-01] · 구현 1단계 ✅ / **Claude OK: 2026-08-05**

- **범위**: A-1-R1 Handoff — `KELLY_THROTTLE_MULT*` 읽기 + SSOT 헬퍼 · tier **쓰기** 미변경
- **발견 (동일 패턴)**:
  - `meta_governor_consumer._resolve_performance_budget_mult`: `get(key, 1.0) or 1.0` → **0.0→1.0 오독** (수정됨)
  - `performance_budget_governor.resolve_position_quota_mult`: A-3 세션에서 이미 수정
- **수정**:
  - `performance_budget_governor.resolve_config_float()` SSOT 신설 (falsy 0.0 치환 금지)
  - `resolve_kelly_throttle_mult()` SSOT 신설
  - `_resolve_performance_budget_mult` → governor SSOT 위임
  - `resolve_position_quota_mult` → `resolve_config_float` 사용
- **grep 전수 감사 (PERFORMANCE_BUDGET 읽기 chain — KR/US forward)**:

  | # | 위치 | 키/함수 | falsy 0.0 위험 | 조치 | MDD 영향 |
  |---|------|---------|---------------|------|----------|
  | 1 | `_resolve_performance_budget_mult` (A-2) | `KELLY_THROTTLE_MULT_{KR\|US}`, `KELLY_THROTTLE_MULT` | **있었음** `get(..) or 1.0` | ✅ R1 → `resolve_kelly_throttle_mult()` | **🔴 헌법급** — sizing 0→1 오독 |
  | 2 | `resolve_position_quota_mult` (A-1/A-3) | `POSITION_QUOTA_MULT_{KR\|US}` | **있었음** | ✅ R1 → `resolve_config_float()` | 쿼터 개수 (bool 게이트로 부분 완충) |
  | 3 | `is_block_new_entries` | `BLOCK_NEW_ENTRIES_*` bool | **없음** (bool/str) | — | 신규 진입 **주 차단** |
  | 4 | `forward/shared.py` try_add | import만 | N/A | 변경 없음 | — |
  | 5 | `meta_governor_consumer` L234 | `META_GLOBAL_KELLY_MULT` | 있음 (`or 1.0`) | **A-1-R1 범위 밖** (meta) | A-2 meta chain — 별도 감사 후보 |
  | 6 | bitget `forward/ledger` | `WEIGHT_S1/S4` | 있음 | **A-5 예정** | — |

  **Kelly throttle (A-2) 확인 완료** — Claude Pro 우려 항목: `_resolve_performance_budget_mult` **동일 버그 있었고 R1에서 수정·테스트됨**.

- **Kelly 회귀 테스트 (LOCKDOWN mult=0)**:
  - `test_resolve_performance_budget_mult_zero_not_misread`
  - `test_lockdown_kelly_mult_zero_step4_return` — `apply_meta_kelly_merge(..., meta=None)` → **0.0**
- **LOCKDOWN 2중·3중 게이트 (의도된 다중 방어)** — `00` SSOT 용어집 「LOCKDOWN_DEFENSE_LAYERS」 참조
- **신규/수정 파일**: `performance_budget_governor.py`, `meta_governor_consumer.py`, `tests/test_a1_r1_lockdown_mult_read.py`
- **테스트**: 7 passed (R1) + 12 passed (A-3) = **19 passed**
- **잔여**: [x] Claude Pro OK (2026-08-05) · [ ] 디렉터 Critical 승인 · [ ] **A-1-R2** `META_GLOBAL_KELLY_MULT` L234 falsy (Claude 권고)

### A-1 부수 발견 · POSITION_QUOTA_MULT **읽기** 버그 — [2026-08-01, A-3 세션] · **→ A-1-R1에서 통합 수정**

- **범위**: A-3 Handoff **밖**. `evaluate_performance_budget` / tier **계산·config_kv 쓰기(A-1)** 는 **미변경**.
- **위치**: `performance_budget_governor.resolve_position_quota_mult()` (읽기 전용 헬퍼)
- **수정 전**: `float(cfg.get(key, 1.0) or 1.0)` → 키가 `0.0`이면 Python falsy로 **`1.0`으로 오독**
- **수정 후**: 키 존재 시 `float(raw)` 그대로 → `0.0` 유지
- **실전 함의**:
  - LOCKDOWN tier는 `block_new_entries=True` + `is_block_new_entries()` **선행 게이트**(try_add ~1733)가 **별도** 동작 → 신규 차단 **주 경로는 살아 있었을 가능성**
  - gate #6 `resolve_max_open_positions(mult=0)` 경로는 **깨져 있었음** (0.0→1.0 오독 → base×1 ≈ 20 OPEN 허용)
- **상태**: A-1-R1에서 `resolve_config_float` SSOT로 **통합 처리** (아래 A-1-R1 섹션)

### A-4 비대칭 히스테리시스 — [2026-08-06] · 구현 1단계 ✅ / **Claude OK: 2026-08-06**

- **Claude OK (2026-08-06)**: Adapter(`resolve_transition_hysteresis_days`)/6쌍/crisis bypass/killswitch(`ENABLE_ASYMMETRIC_HYSTERESIS`)/UNKNOWN fallback 전부 확인. **16 passed**. 비고(블로킹 아님): 6쌍 명시목록·`HYSTERESIS_MIN_DAYS` 재사용 여부 문서 한 줄 추가 권고.
- **Adapter**: `resolve_transition_hysteresis_days()` — 위험확대 6쌍 → 1일 · 축소/UNKNOWN → RL `rl_days` · `ENABLE_ASYMMETRIC_HYSTERESIS` 킬스위치 (default True)
- **6쌍 SSOT**: BULL→SIDEWAYS/HIGH_VOL/BEAR · SIDEWAYS→HIGH_VOL/BEAR · HIGH_VOL→BEAR
- **호출부**: `run_regime_ensemble` 히스테리시스 루프 — `crisis`/`crisis_synced`는 `force_immediate` 유지(어댑터 우회)
- **RL 축**: `evolve_hysteresis()` EMA/클램프 **무변경**
- **UNKNOWN grep**: `_stress_merge` fallback `"UNKNOWN"` · `blended_regime` empty→`UNKNOWN` · 개별 시장 `_regime_from_score`는 BULL/SIDEWAYS/BEAR만 — **UNKNOWN 전환은 축소=RL fallback**
- **신규/수정**: `predictive_regime_ensemble.py`, `tests/test_asymmetric_hysteresis_a4.py`
- **테스트**: 16 passed
- **grep 부가 등록(미수정)**: L243 clutch `or 1.0` → **A-1-R3** 후보 (즉시 착수 가능)
- **잔여**: [x] Claude Pro OK (2026-08-06) · [ ] **디렉터 Critical 승인 (A-4 배포)** · [ ] 2~4주 `06` 효과 검증

### A-1-R2 · META_GLOBAL_KELLY_MULT falsy — [2026-08-05] · 구현 1단계 ✅ / **Claude OK: 2026-08-05**

- **Claude OK (2026-08-05)**: L234 `resolve_config_float(default=1.0)` 확인. grep 부가 4건 **수용(등록만)**: L243(🔴 **A-1-R3 후보**) · L411/L73(🟡 후속 후보) · doomsday_dampener(🟢 위험없음, 종결).
- **범위**: `meta_governor_consumer.apply_meta_kelly_merge` L234 — `META_GLOBAL_KELLY_MULT` 읽기 1곳
- **수정**: `float(meta.get(...) or 1.0)` → `resolve_config_float(meta, "META_GLOBAL_KELLY_MULT", default=1.0)`
- **grep 부가 보고 (이번 세션 미수정)**:
  - L73 `DYNAMIC_KELLY_RISK` `or 0.01` (sys_config) — 🟡 후속 후보
  - L243 `REGIME_TRANSITION_CLUTCH.mult` `or 1.0` — 🔴 **A-1-R3 후보**
  - L258~268 `META_NS_KELLY_MULT` / `META_GROUP_KELLY_MULT` — `float(dict[key])` 직접 (0.0 유지, or 패턴 없음)
  - L411 `MAX_POSITION_PCT` `or 0.25` (`effective_max_position_pct`) — 🟡 후속 후보
  - `doomsday_dampener.py` L189 `or 0.0` (DB row) — 🟢 위험없음, 종결
- **신규/수정 파일**: `meta_governor_consumer.py`, `tests/test_a1_r2_meta_global_kelly_mult.py`
- **테스트**: 2 passed (R2) + 7 passed (R1 회귀)
- **잔여**: [x] Claude Pro OK (2026-08-05) · [ ] **디렉터 Critical 승인 (A-1-R2 배포)** · [ ] 2~4주 `06` 효과 검증

### A-1-R3 · REGIME_TRANSITION_CLUTCH.mult falsy — [2026-08-06] · 구현 1단계 ✅ / **Claude OK: 2026-08-06**

- **Claude OK (2026-08-06)**: L249 `resolve_config_float(clutch, "mult", default=1.0)` — A-1-R2 패턴 일치. mult=0.0 clutch disengage 보존(의도). L249 롤백 조건 일치. 13 passed. grep L73/L417 수용(미수정).
- **수정**: `float(clutch.get("mult", 1.0) or 1.0)` → `resolve_config_float(clutch, "mult", default=1.0)` (L234 import 재사용)
- **grep 부가 보고 (이번 세션 미수정)**:
  - L73 `DYNAMIC_KELLY_RISK` `or 0.01` — 🟡 후속 후보
  - L417 `MAX_POSITION_PCT` `or 0.25` — 🟡 후속 후보
  - `doomsday_dampener.py` — 🟢 종결 (R2 등록 유지)
- **신규/수정 파일**: `meta_governor_consumer.py`, `tests/test_a1_r3_regime_transition_clutch.py`
- **테스트**: 4 passed (R3) + 2 passed (R2) + 7 passed (R1 회귀) = **13 passed**
- **잔여**: [x] Claude Pro OK (2026-08-06) · [ ] **디렉터 Critical 승인 (A-1-R3 배포)** · [ ] 2~4주 `06` 효과 검증

### A-1-R4 · DYNAMIC_KELLY_RISK + MAX_POSITION_PCT falsy — [2026-08-06] · 구현 1단계 ✅ / **Claude OK: 2026-08-06**

- **Claude OK (2026-08-06)**: L73 except-fallback / L417 position cap → `resolve_config_float` 확인. `MAX_POSITION_PCT=0.0` kill 의도 보존. Bitget/리포트 트랙 분리 적절(Rule1). import L14 단일화. **19 passed**. `meta_governor_consumer` or-패턴 0건.
- **수정**: `float(...get(...) or default)` → `resolve_config_float(..., default=0.01|0.25)` — 모듈 import 1곳(L14)으로 통일, `apply_meta_kelly_merge` 중복 import 제거
- **grep 부가 보고 (이번 세션 미수정 · KR/US 외)**:
  - `reports/report_state_binder.py` L199/L210 `DYNAMIC_KELLY_RISK` or 0.01 — 🟡 후속
  - `overseer_audit_binder.py` L263 — 🟡 후속
  - `bitget/governance/meta_consumer.py` L87/L229 — 🟡 Bitget 트랙 별도
  - `bitget/pipelines/bitget_pipelines.py` L416 · `bitget/trading/regime_capital_relay.py` L123 — 🟡 Bitget
- **`meta_governor_consumer.py`**: `or 0.xx` / `or 1.0` 패턴 **0건** (R1~R4 SSOT 완료)
- **신규/수정 파일**: `meta_governor_consumer.py`, `tests/test_a1_r4_kelly_base_and_position_cap.py`
- **테스트**: 6 passed (R4) + 13 passed (R1~R3 회귀) = **19 passed**
- **잔여**: [x] Claude Pro OK (2026-08-06) · [ ] **디렉터 Critical 승인 (A-1-R4 배포)** · [ ] 2~4주 `06` 효과 검증

### A-5a S5 arm 배분 (CAT-F) — [2026-08-06] · 구현 1단계 ✅ / **Claude OK: 2026-08-06 (rev.2 — TOXIC_FADE 단독 제외 반영 후)**

- **범위**: `resolve_defense_arm_weight` · `forward/shared.py` Step1 S5 · `ACTION_BY_REGIME.weight_s5_bounds` · `classify_strategy_arm→S5` · `mab` arm `"S5"`
- **Config**: `WEIGHT_S5`(default 1.0) · `ENABLE_WEIGHT_S5_MERGE`(default True) · `PERFORMANCE_BUDGET_DEFENSE_ARM_ACTIVE_*` 읽기만
- **S5 sig SSOT (rev.2)**: `[INVERSE_ETF]`/INVERSE_ETF · BLACKHOLE/BLACK_HOLE **만** — **TOXIC_FADE 단독 제외** (CAT-I 역이용, Handoff 밖 · Claude 지적 반영)
- **forward_trades S5 sig**: 로컬 DB 없음 · fade 실거래는 `[INVERSE_ETF]` 동시 부착으로 S5 커버
- **비접촉**: `inverse_etf_sniper.py` · `blackhole_hunter.py` (CAT-C)
- **테스트**: **14 passed** (rev.2 — TOXIC_FADE 단독 제외) · R3/R4 회귀 10 passed
- **롤백**: `ENABLE_WEIGHT_S5_MERGE=False` → Step1 S5 스킵 · BH 라벨 `"BH (블랙홀)"` 복귀 (**A-4/R 플래그와 독립**)
- **배포**: R2·R3·R4·A-4·A-5a **일괄** — A-5a Claude OK 후 `update_factory.sh` (디렉터 2026-08-06)
- **잔여**: [x] Claude Pro OK (2026-08-06 rev.2) · [ ] **디렉터 Critical 승인(일괄 배포)** · [ ] A-5b(G) · [ ] A-5c(C)

**Phase 1 완료** — [____] → Phase 2 시작 가능 여부: **아니오** (A-3~A-5 + 3단계 검증 잔여)

---

## Phase 2 · 묶음B

### B-1 유전자풀 — [날짜: ____]
- **로컬 현행**: `PRICE_VARS=(open,high,low,close,ma5,ma10,ma20)`, `VOL_VARS=(volume,vol_ma5,vol_lag1)` — mean-reversion/vol structure **미추가**
- 추가 변수 목록:
- 신규/수정 파일:

### B-2 GP 마찰 — [날짜: ____]
- λ_turnover, λ_slip:
- synthetic slippage on/off:
- 신규/수정 파일:

### B-3 데스매치 킬 — [날짜: ____]
- **로컬 현행**: `deathmatch_config.py` lookback KR **90d**, US **120d** — 45d·-8% 즉시 STANDBY **미구현**
- lookback (일):
- 실시간 MDD 킬 임계 (%):
- 신규/수정 파일:

### B-4 S5 lifecycle — [날짜: ____]
- registry group_key:
- promotion 테스트 결과:
- 신규/수정 파일:

**Phase 2 완료** — [날짜: ____]

---

## Phase 3 · 묶음C

### C-1 섹터/스필오버 — [날짜: ____]
- Markov 차수:
- spillover lag (일):
- 신규/수정 파일:

### C-2 유니버스/캐시 — [날짜: ____]
- TTL 변경:
- 병목 결론:
- 신규/수정 파일:

### C-3 bitget 선행신호 — [날짜: ____ / No-Go]
- Go/No-Go:
- factor 이름 + shadow 기간:
- 신규/수정 파일:

**Phase 3 완료** — [날짜: ____]

---

## Phase 4 · 묶음D

### D-1 AI CIO — [날짜: ____]
- 모듈 경로:
- JSON schema 확정:
- 신규/수정 파일:

### D-2 승인 게이트 — [날짜: ____]
- audit SSOT (table/key):
- auto-approve bounds 참조:
- 신규/수정 파일:

### D-3 DSR 루프 — [날짜: ____]
- walk_forward hook:
- 4주 스케줄 (mode/cron):
- 신규/수정 파일:

**Phase 4 완료 · 로드맵 종료** — [날짜: ____]

---

## 인프라 · work_phases 이식 — [2026-08-03]

- `00_마스터_로드맵` · `09` · `11` · `NEXT_STEP` KR/US 이식
- **bitget 대칭 보완**: `ARCHITECT_MIRROR.md` · `09` 비개발 한 장 확장 · `README`·`NEXT_STEP` 체크리스트 형식 정렬 (파일 20개 = bitget `work_phases` 목록 동일)

---

## CAT-M · M-R0 상시 감독관 품질 — [2026-08-04] · 복구·구현 ✅ / **Claude OK: 2026-08-05**

- **Handoff**: Claude Pro M-R0 (비침투·D-1/D-2 경계)
- **변경**: `llm_provider_core.py`, `overseer_quality.py`, `ai_overseer.py`, `factory_runtime.py`, `llm_gemini_core.py`(provider cache key), `bitget/overseer_audit_contract.py`
- **config**: `LLM_PROVIDER_MODEL_MAP`, `LLM_PROVIDER_ANTHROPIC_ENABLED` (기본 False)
- **테스트**: `test_llm_provider_core_mr0.py`, `test_overseer_quality_mr0.py` + ch6 — 22 passed
- **잔여**: [x] Claude OK (2026-08-05) · [ ] Anthropic 서버 Go · [ ] Bitget M-R0-4 full (계약만)
